// In-app Legal route: renders the verbatim OGL 1.0a, the per-system open-content
// attributions, the CC-BY-4.0 legal code (SRD 5.2), and the AI/affiliation
// disclaimer. All text comes from src/legal/legalText.ts (generated by
// scripts/fetch-legal-texts.mjs from canonical sources) — none is hand-authored
// here. This satisfies the OGL Section-10 "reproduce the License" obligation and
// the LEGAL-1 launch-blocker (license text reachable in the running app).
import { LEGAL_SECTIONS } from '../legal/legalText';

export interface LegalNoticesProps {
  /** Optional handler for a back/close affordance when shown as a full view. */
  onClose?: () => void;
}

export default function LegalNotices({ onClose }: LegalNoticesProps) {
  return (
    <div className="mx-auto max-w-4xl p-6" data-testid="legal-notices">
      <div className="mb-6 flex items-center justify-between">
        <h1 className="text-2xl font-bold text-foreground">Legal & Open-Content Notices</h1>
        {onClose && (
          <button
            type="button"
            onClick={onClose}
            className="rounded-md border border-border px-3 py-1.5 text-sm hover:bg-muted"
          >
            Close
          </button>
        )}
      </div>

      <p className="mb-6 text-sm text-muted-foreground">
        This application uses only open-game / open-content material. The full license texts and
        per-system attributions are reproduced below. This is an independent, fan-made tool and is
        not affiliated with Wizards of the Coast, Paizo Inc., Green Ronin Publishing, or Darrington
        Press LLC.
      </p>

      <nav className="mb-6" aria-label="Legal sections">
        <ul className="flex flex-wrap gap-2 text-sm">
          {LEGAL_SECTIONS.map((section) => (
            <li key={section.id}>
              <a
                className="text-primary underline hover:no-underline"
                href={`#legal-${section.id}`}
              >
                {section.title}
              </a>
            </li>
          ))}
        </ul>
      </nav>

      {LEGAL_SECTIONS.map((section) => (
        <section key={section.id} id={`legal-${section.id}`} className="mb-8">
          <h2 className="mb-2 text-lg font-semibold text-foreground">{section.title}</h2>
          <pre className="overflow-x-auto whitespace-pre-wrap rounded-md border border-border bg-muted/40 p-4 text-xs leading-relaxed text-foreground">
            {section.body}
          </pre>
        </section>
      ))}
    </div>
  );
}
