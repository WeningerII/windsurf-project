# UI redesign — build-ready phase specs (re-grounded)

**Date:** 2026-07-08 · **Basis:** `ui-shell-redesign-final-plan.md` (plan of record). Produced by a workflow that re-verified every file:line anchor against the CURRENT source, expanded each phase into ordered build tasks + test blast radius + runnable acceptance gates, verified cross-phase sequencing, and emitted a Phase-1 kickoff. **No code changed.**

The point of this pass was to catch drift between the plan and reality before a builder trusts an anchor. It did. **Phase 1's anchors are 100% still valid** (build-ready now); the corrections below matter for the later phases.

## Drift corrections to the plan of record (apply these before building the affected phase)

- **`src/context/` → `src/contexts/` (PLURAL).** The plan's file lists put ShellContext / SheetDispatchContext / SceneDispatchContext under `src/context/` (singular). The repo convention AND the vitest coverage `include` glob are `src/contexts/` — landing them singular would drop them from coverage and break the import convention. Affects Phases 2, 3, 4, 7.
- **RUNTIME_COPY_RULES is FIFTEEN entries, not thirteen** (`docs/doc-drift.rules.ts` L291-391). The plan (and the tournament) said 13. The Phase-7 constraint-of-record must state 15 (or derive from `.length`), and Phase 5's guard blast radius is counted against 15. All six edited-not-deleted guarded files are still correctly identified.
- **The bestiary/monster tab is doc-drift-SAFE.** The plan feared `Dnd5eMonsterBrowserTab.tsx` might be guarded and require a paired rules.ts edit — it is in NEITHER rules.ts nor manifest.ts, so deleting it is simpler than planned. BUT its plumbing threads FOUR files (Dnd5eSheetBase, Dnd5eTabsNavigation, the tab, and the loader wiring), not 2 — broader delete than the plan said. `loadMonstersForSystem` stays live (used by the scene encounter path), so no knip fallout. Affects Phase 3.
- **`ConfirmDialog` is strictly two-action** (confirm/cancel, props L6-15). Phase 4 planned to reuse its plumbing for the friendly/hostile allegiance chip — it can't be reused as-is; the chip needs a new small component (or a genuine extension of ConfirmDialog). Affects Phase 4.
- **doc-drift is THREE coupled files, not two:** `docs/doc-drift.rules.ts` + `docs/doc-drift.manifest.ts` + `src/__tests__/docDriftManifest.test.ts` (which asserts the rule set). Any guarded-file edit pairs with all three. Affects Phase 5.
- **pf2e / d20-legacy spell 'browser panels' are EMBEDDED, not standalone wrappers.** The plan listed them as 'delete wrappers'; they're embedded in their tabs, so the collapse is an edit, not a clean delete. Affects Phase 5.
- **`assertNever` lives ONLY in the frozen `src/scene/runtime.ts`.** Do not cross-import it into `useAppNav` (that couples shared→scene and touches the frozen file's import graph). Define a local one. Affects Phase 1.
- **`hostSizeBudget` ≤400-LOC ceiling applies to the 5 SHEET hosts only** — NOT SceneSurface/SceneManager. This REDUCES Phase 6's constraint burden (the scene decomposition is not host-LOC-gated). Affects Phase 6.
- **Anchor mislabels (immaterial, corrected):** useEntitySync `sync()` is L110-196/returned L334 (plan said 'L133/L162'); `buildPlacedToken` spans L36-70 (plan said :36-68); the feat `browserSupport` pin is L353-354 (plan said L355); `GameSystemSelector.test.tsx` path differs slightly. None reorder anything.

## Cross-phase sequencing — CLEAN DAG (verified)

**Acyclic: True · Violations: 0.** The topological order 1→2→3→4→5→6→7 is valid — every prerequisite a phase consumes is produced by a strictly-earlier phase (the red-team's recurring 'phase X needs what phase Y builds later' class is absent here). Notable facts:

- **P4 and P5 are independent siblings** (both depend only on P3; neither on the other) — P5 (sheet-only, drag-free) could parallelize with P4 (scene/drag) once P3 lands. Linear numbering is a safe superset of the true partial order.
- **Two distinct dispatch contexts, no confusion:** `SheetDispatchContext` (P3, dock→sheet click-add) and `SceneDispatchContext` (P4, zero-arg scene emit) are separate files with separate consumers.
- **Invariant held across all edges:** no phase edits `runtime.ts` or the 12 intents; `onLogToCampaign` stays an App callback (never a 13th intent) as its ownership passes P1→P6.

**Gate points:**
- PHASE 4 — FUND/KILL PROTOTYPE GATE (primary, the whole plan's viability checkpoint). Evaluated on a TRANSFORMED ~900-cell spike (not the trivial untransformed grid, not a hardcoded default allegiance): two sub-gates + a probe. 3b-i (party character 1-choice auto-apply) and 3b-ii (monster friendly/hostile chip) must each meet an explicit post-drop reconcile FRAME budget; the dynamic-viewport probe (variable scale+translate inversion, wheel-zoom-to-cursor, continuous-repaint budget, touch pinch/pan) must show continuous repaint is reachable. DECIDES: whether the drag-interaction thesis is funded before the expensive scene work. SCOPING NUANCE the reviewer must ratify: the spec says the gate gates 'Phases 5-6', but Phase 5 is click-to-add sheet eviction with NO drag and depends only on Phase 3 — so the fund/kill most tightly gates PHASE 6 (canvas/drag/viewport). If the gate KILLS the drag thesis, Phase 5 can still proceed; only the scene-canvas program is halted/rescoped. OPEN INPUT: the concrete ms/FPS threshold is left to the Phase-4 measurement and must be ratified by the owner before task 11 can pass/fail.
- PHASE 6 5d — PAN/ZOOM VIEWPORT OWN GATE (secondary, distinct from the render swap). SceneViewport carries its OWN acceptance gate: zoom-to-cursor lands the correct integer cell under variable scale+translate, and continuous pan/zoom repaint meets the frame budget on ~900 transform-positioned cells. Phase 4 only PROBED this math; Phase 6 5d is where it actually ships-or-doesn't. DECIDES: whether the pan/zoom subsystem is delivered independently of the fixed-1:1 SceneCanvas render (5c-i can land without 5d). Its threshold is INHERITED from the Phase-4 spike output — a real prerequisite: the number must exist from P4 before the 5d gate can be asserted.
- PHASE 7 — OWNER USABILITY GATE (human sign-off). The owner's per-chapter 'feels buttery' sign-off is recorded as the explicit gate for whether the NEXT interaction chapter (specifically the 5d pan/zoom subsystem) is warranted. DECIDES: go/no-go on further interaction investment; complements the two automated gates (interaction-budget + chrome-dominance) that P7 wires into `npm run verify` after test:e2e.
- PHASE 1 — ATOMIC-LANDING GATE (coherence, not fund/kill). Explicitly ONE atomic PR: 'no sub-task lands alone'. The 6 sheet-open writers, 3 close-sheet sites, 5 scene-selection writers, LibraryScenesView, NewCharacterDialog, rail relocation and ALL rewritten render(<App/>)/direct-SceneManager suites must merge together or the total nav union is half-wired. This is the seam foundation every later phase consumes, so its all-or-nothing landing protects every downstream edge.
- PER-PHASE PREREQUISITE GATES (build-order enforcement, already spec'd): P4 task 1 confirms P1-3 merged before wiring drag; P5 task 0 confirms P3's SheetDispatchContext + dock exist before evicting in-sheet browsers (else click-add is stranded); P6 is documented as blocked until P1/P2/P4 deliver SceneSurface + emit seam + drag engine + chip; P7's chrome-dominance gate is anchored to the post-Phase-6 5b summoned-tray layout (it would falsely fail against the P1 docked 20rem rail). Each of these is a hard STOP-if-absent checkpoint, and each names an EARLIER producer — confirming the DAG.
- PER-PHASE CI GATE (`npm run verify` green) is the uniform release gate on every phase: eslint layer-boundary (shared must not value-import src/systems/**; inversion via SheetDispatchContext/SceneDispatchContext keeps this clean), knip dead-code (new seam exports must land WITH their consumer — e.g. P4's emit seam ships with its DragLayer consumer), hostSizeBudget (5 named sheet hosts ≤400 LOC — only P3/P5 touch a host, both net-neutral/down), doc-drift (only P1 keeps GameSystemSelector+SystemStatusDashboard alive and P5 pairs the Dnd5eFeatBrowserTab delete across rules.ts+manifest.ts+docDriftManifest.test.ts; P2/P4/P6/P7 touch no guarded path), coverage 70/65/60/70, bundle-size (SceneManager stays lazy out of the eager index chunk across P1/P2).

## Phase-1 kickoff — the actual first commit

**Landing model:** Phase 1 MUST land as ONE atomic PR (the plan states "no sub-task lands alone" and knip/coverage/doc-drift/the rewritten render(<App/>) + direct-SceneManager suites all break the moment any single writer is repointed). So there is no independently-mergeable sub-PR. The correct FIRST COMMIT on the phase-1 branch is task 1 in isolation: the net-new `useAppNav` discriminated-union hook + its unit test. It is pure/self-contained, imports nothing from src/systems/** (layer-boundary safe), and depends on no other task — but it will NOT pass `npm run verify` alone because knip flags it as an unused export until App.tsx consumes it (task 2). So: commit 1 = useAppNav + test (green under `npm test`, red under knip-in-verify by design); the branch stays open until the full atomic PR is assembled. Rationale for starting here: every downstream writer (6 openSheet, 3 closeSheet, 5 scene-selection) and every rewritten suite consumes this union, so building and type-proving the seam first (assertNever over BOTH discriminants) de-risks the entire PR before a single call site in App.tsx is touched.

**Files to create:** `src/hooks/useAppNav.ts`, `src/hooks/__tests__/useAppNav.test.ts`

**Files to edit (this PR, later commits):** `src/App.tsx`

**First test:** src/hooks/__tests__/useAppNav.test.ts — renderHook(useAppNav) asserting: (1) initial state {surface:'library', librarySegment:'characters', sheetDocId:null, sceneId:null, overlay:null}; (2) openSheet('doc-1') -> {surface:'sheet', sheetDocId:'doc-1'}; (3) closeSheet() from a sheet -> {surface:'library', librarySegment:'characters'} (default segment restored, sheetDocId null, no stale id); (4) onSelectScene('scene-9') sets sceneId:'scene-9' AND surface:'scene' (the compound create/import seam); (5) onSelectScene(null) clears sceneId; (6) setSurface / setLibrarySegment round-trip; (7) a type-level `// @ts-expect-error` block proving the LOCAL assertNever(x:never) fails to compile if a surface or librarySegment discriminant case is dropped (the frozen-core-safe local helper, NOT an import from src/scene/runtime.ts).

**First commit message:** `feat(nav): add useAppNav total shell-nav discriminated union (Phase 1 seam)`

**Do NOT yet (later tasks/phases):**
- Do NOT repoint any of the six setCurrentDocId sheet-open writers or the three setCurrentDocId(null) close sites in App.tsx yet (task 2) — that lands after the hook is type-proven, and it hard-breaks the four render(<App/>) suites, so it must land WITH their rewrites in the same PR.
- Do NOT lift SceneManager scene-selection to controlled props (task 3) or delete its L884-915 LEFT rail body — that must land in the same commit as the SceneManager.test.tsx + capabilityScenarios.test.tsx rewrites (task 12), not before.
- Do NOT create LibraryScenesView, NewCharacterDialog, CampaignManager/SystemStatusDashboard re-hosting, or the AppHeader center-nav yet (tasks 5-10) — they consume the hook that this commit only just establishes.
- Do NOT import assertNever from src/scene/runtime.ts — that file is frozen; define a local one in the hook to avoid shared->scene coupling and the layer-boundary lint.
- Do NOT touch docs/doc-drift.rules.ts or the manifest — the default Phase-1 path keeps GameSystemSelector + SystemStatusDashboard alive with their tokens, so those FILE-PATH-keyed guards stay UNCHANGED.
- Do NOT delete the App.tsx L513-519 hero / L552-563 action bar / L511-648 cascade in this first commit (task 10) — it strands the sheet-open writers and the flow-suite copy assertions until the whole PR is assembled.
- Do NOT wire lazy-mount-on-first-visit/keepalive, the perf harness, or the bundle-chunk assertion (tasks 4/11) yet — SceneManager stays React.lazy() at App L30-31 and this commit changes no mount behavior.
- Do NOT touch src/scene/runtime.ts, the 12 SceneActionIntent variants, onLogToCampaign (App handleLogSceneToCampaign L132-141), or onAppendSceneEvent — invariant across all of Phase 1.
- Do NOT convert to ShellContext (that is Phase 2) — keep the useState/useReducer internals behind the stable surface API.

---

# Per-phase build specs

## Phase 1 — Handoff frame + total nav seam (useAppNav discriminated union) + compound openSheet (6 writers) + compound scene-selection (5 writers) + lazy-mount-on-first-visit SceneManager + Export/Delete re-home + LEFT-rail-only Scene declutter + full test blast radius. ONE atomic PR (no sub-task lands alone).

**Anchors:** 16 verified, 0 drifted. **Definition of done:** "ONE atomic PR merges with npm run verify green. The L511-648 home cascade is replaced by header-tab Library segment views (characters default) backed by the TOTAL useAppNav discriminated union (assertNever over both discriminants compiles). All SIX sheet-open writers route through openSheet(docId) and the three setCurrentDocId(null) sites through closeSheet (flushes preserved); opening a character from list/clone/import/campaign-card/header-switcher reliably shows the sheet. Scene selection is a controlled five-writer seam (init+auto-reset shell-owned, rail-click via onSelectScene, create/import relocated into LibraryScenesView dispatching onSelectScene+surface:'scene'); creating/importing a scene lands on the live canvas. A select-only LibraryScenesView (distinct from the Scene canvas) is the scene home; the Scene surface's LEFT 18rem list rail is relocated (grid widens) while the RIGHT 20rem operating rail stays docked with every panel in place. SceneManager stays React.lazy() and lazy-mounts-then-keepalives on FIRST Scene visit via visibility:hidden+off-screen (never display:none/content-visibility:auto), so index-chunk size/TTI are preserved and transient combat/placement/initiative state survives Scene↔other switches. New Character (dialog) + Import are always reachable; per-character Export+Delete are re-homed to the Sheet-surface header overflow AND per-card in CharacterListView. GameSystemSelector + SystemStatusDashboard stay alive with doc-drift tokens intact (rules.ts/manifest.ts unedited); legal strings preserved. onLogToCampaign + onAppendSceneEvent unchanged; src/scene/runtime.ts and the 12 SceneActionIntent variants untouched. Every render(<App/>) suite and both direct-SceneManager-mount suites are rewritten in-commit, and the eight Playwright acceptance gates pass."

**Drift found:**
- doc-drift RUNTIME_COPY_RULES count: the plan (and this task brief) say 'THIRTEEN paths (L295-381)'. ACTUAL: the array is 15 entries spanning L291-391 (adds scripts/runtime/runtime-policy.mjs L292, scripts/check-node-version.mjs L302, and src/utils/documentationCopy.ts L379 on top of the 12 src/system files enumerated). Every individually-cited line anchor still holds exactly (GameSystemSelector L325-327, SystemStatusDashboard L330-332, D20SpellsTab L335, Dnd5eSpellsTab L340, Dnd5eSelectedFeatsSection L348, Dnd5eFeatBrowserTab browserSupport L355, Dnd5eFeatureOptionsSection L358, Pf2eSpellsTab L366, MamArchetypesTab L374). Correct the count to 15 in the Phase-7 seam doc; Phase-1 impact is nil (only the 2 Phase-1-relevant guarded files are affected, both verified).
- GameSystemSelector.test.tsx path: the plan's Phase-1 file list implies src/__tests__/GameSystemSelector.test.tsx. ACTUAL path is src/__tests__/components/GameSystemSelector.test.tsx. Update mocks there.
- Alt+N repoint location: the plan lists src/hooks/useKeyboardNavigation.ts as the edit site. ACTUAL: useKeyboardNavigation.ts is a generic hook taking a shortcuts array; the Alt+N (App.tsx L379-385) and Ctrl+I (L387-393) shortcut DEFINITIONS live in App.tsx's useKeyboardNavigation([…]) call (L368-411). The repoint + new Alt+1/2/3 surface switches are edited in App.tsx; the hook file itself likely needs no change.
- assertNever source: an assertNever helper exists only in src/scene/runtime.ts (the frozen file). Do NOT cross-import it into useAppNav.ts; define a local assertNever in the hook to keep the frozen-core invariant clean and avoid shared→scene coupling.
- currentDocId is a useState in App.tsx (L91), not yet any nav abstraction — confirms the union is net-new; deriving currentDoc from nav.sheetDocId replaces L91/L413. No hidden pre-existing nav state to reconcile.

**Tasks (ordered):**
1. Create the TOTAL nav union FIRST. Define ShellNavState = {surface:'library'|'sheet'|'scene', librarySegment:'characters'|'campaigns'|'scenes'|'content', sheetDocId:string|null, sceneId:string|null, overlay:'legal'|null} with a LOCAL `assertNever(x:never):never` (do NOT import the one in src/scene/runtime.ts — that file is frozen and cross-importing it from a hook is unnecessary coupling) switched over BOTH the surface and librarySegment discriminants so a missing case fails typecheck. Expose openSheet(docId)=>{surface:'sheet',sheetDocId:docId}, closeSheet()=>{surface:'library'} (default segment 'characters'), setSurface, setLibrarySegment, and the compound scene-selection seam selectedSceneId + onSelectScene(id:string|null) that BOTH sets sceneId AND (for create/import callers) dispatches surface:'scene'. Back it with useState/useReducer internally — Phase 2 swaps internals for ShellContext, so keep the surface API stable. Ship src/hooks/__tests__/useAppNav.test.ts in this PR (openSheet result, onSelectScene sets sceneId+surface, closeSheet returns to characters, assertNever compiles — a type-level @ts-expect-error test). _(atomic w/ siblings)_
2. Rewire all SIX sheet-open writers in App.tsx to nav.openSheet(docId) — do NOT mechanically port to a bare set: handleCreateCharacter (replace setCurrentDocId(doc.id) L276), handleCloneDocument (L328), handleImportDocument (L346), AppHeader onSelectCharacter (L435), CharacterListView onOpenCharacter (L585), CampaignManager onOpenCharacter (L618). Map the three setCurrentDocId(null) sites to nav.closeSheet(): handleReturnToList (L190 — KEEP the three flushPending*Saves calls L187-189 before closeSheet), handleDeleteDocument (L286, keep the `if(currentDocId===id)` guard → closeSheet), onClearAll (L580). Derive currentDoc from nav.sheetDocId (replace currentDocId state L91). Keep updateDocument/exports unchanged. _(atomic w/ siblings)_
3. Lift scene selection to a controlled seam in SceneManager, BEHAVIOR-PRESERVINGLY and BEFORE LibraryScenesView consumes it (mandatory ordering). Add props selectedSceneId:string|null + onSelectScene:(id:string|null)=>void. Convert the internal writers: init L109 and auto-reset L143 become shell-owned — the effect calls onSelectScene(scenes[0]?.id ?? null) when the current id is stale; rail-click L926 calls onSelectScene(scene.id) (still clearing selectedTokenId/placementMode locally). Leave L807 (select-first-imported) and L872 (select-just-created) calling onSelectScene for now — they RELOCATE in task 5. App passes selectedSceneId={nav.selectedSceneId} onSelectScene={nav.onSelectScene}. This must land in the same commit as the SceneManager.test.tsx + capabilityScenarios.test.tsx edits (task 12). _(atomic w/ siblings)_
4. Keep SceneManager React.lazy() (App L30-31 UNCHANGED) and lazy-mount-on-first-visit + keepalive — do NOT mount from boot. Track a `sceneVisited` boolean in App (set true the first time nav.surface==='scene'); render <Suspense><SceneManager…></Suspense> only once sceneVisited is true, then keep it in the layout tree via visibility:hidden + off-screen transform (NOT display:none, NOT content-visibility:auto) whenever nav.surface!=='scene'. Characters stays the boot landing. Add a Phase-1 performance.measure harness comparing three mount-hide mechanisms (display:none vs visibility-hidden-only vs visibility+content-visibility:auto) on a real ~30x30 grid and default to visibility-hidden-only. _(atomic w/ siblings)_
5. Create LibraryScenesView.tsx — a select-only picker extracting SceneManager's L884-915 rail body (campaign filter + scenes.filter().map() driving onSelectScene) PLUS the existing SceneCreateForm for create/import. RELOCATE the L807 (import→select imported[0]) and L872 (create→select) writers here: SceneCreateForm.onCreate and the import handler now call onSelectScene(newScene.id) AND dispatch surface:'scene' (via nav) so create/import-then-open lands on the live canvas. Remove that rail body + the create/import UI from SceneManager (its LEFT 18rem column L885 collapses; change the grid so SceneGridView+right rail widen by 18rem). SceneManager keeps the RIGHT 20rem operating rail (L1007) DOCKED. This is distinct from the Scene canvas so the picker→canvas IA is real. _(atomic w/ siblings)_
6. Create NewCharacterDialog.tsx (portaled) rendering systemRegistry.getAll() rows with systemAccents reuse and one-click create; move selectedSystem state (App L92) into it. KEEP GameSystemSelector.tsx ALIVE unchanged as the doc-drift token host (Finding 12 — the guard is FILE-PATH-keyed to src/components/GameSystemSelector.tsx L325-327, so relocating its supportNotes/supportBadge* rendering into the dialog does NOT satisfy it). The dialog is a separate create surface; GameSystemSelector still renders somewhere (e.g. inside the dialog or a content view) with its three tokens intact. _(atomic w/ siblings)_
7. AppHeader.tsx: add the always-visible center nav (segmented Library/Sheet/Scene switcher + librarySegment tabs), a primary 'New Character' button (opens NewCharacterDialog) and an 'Import' control; UNGATE onImport out of the {currentDoc && …} block (L102) so Import is reachable with no sheet open. RE-HOME Export+Delete (Finding 18): move the current character's onExport (L128) + onDelete (L147) into a Sheet-surface header '…' overflow menu (still gated on currentDoc). Keep Clone. Preserve the sync/theme/UserMenu cluster. _(atomic w/ siblings)_
8. CharacterListView.tsx: add per-card Export + Delete + Import to a '…' overflow (Finding 18 — today only bulk onExportAll L89 exists; CharacterCard L113-119 has no per-card export/delete). Per-card delete routes through the App closeSheet path (task 2). Add a name/search filter input. Thread new props (onExportCharacter, onDeleteCharacter, onImport) from App. _(atomic w/ siblings)_
9. Keyboard: in App.tsx's useKeyboardNavigation([…]) call (L368-411 — NOTE the shortcut ARRAY lives in App.tsx, the useKeyboardNavigation.ts hook itself is generic and needs no edit) repoint Alt+N (L379-385) to open NewCharacterDialog instead of handleCreateCharacter, and add Alt+1/Alt+2/Alt+3 to switch nav.surface (library/sheet/scene). Keep Ctrl+I import, Escape/Undo/Redo. _(atomic w/ siblings)_
10. Delete the hero block (L513-519) + centered action bar (L552-563) from App.tsx; replace the L511-648 cascade with exactly ONE Library segment view per nav.librarySegment (characters→CharacterListView, campaigns→CampaignManager, scenes→LibraryScenesView, content→SystemStatusDashboard). Re-host SystemStatusDashboard.tsx as librarySegment:'content' KEEPING its doc-drift tokens (rules.ts L330-332). Give CampaignManager.tsx a two-column view header. Preserve the setShowLegal(true) path + legal strings verbatim, modeled as overlay:'legal' in the union. _(atomic w/ siblings)_
11. Add instrumentation: performance.mark/measure around the surface switch (baseline interaction-latency number for the Phase-7 budget) AND a bundle-size/index-chunk assertion that the SceneManager chunk stays OUT of the eager index chunk (Finding 17 — guards that lazy-mount-on-first-visit did not accidentally de-lazy it). _(atomic w/ siblings)_
12. Rewrite the direct-SceneManager-mount suites in the SAME commit as task 3: SceneManager.test.tsx and capabilityScenarios.test.tsx must pass the controlled selectedSceneId/onSelectScene props, re-assert shell-owned auto-reset (stale id → onSelectScene(scenes[0]?.id ?? null)) and create/import-then-open selection+surface. They break the moment the prop contract changes (Finding 6), so they cannot be deferred. _(atomic w/ siblings)_
13. Rewrite the four render(<App/>) flow suites (Finding 5): character-creation-flow.test.tsx (createCharacter helper L38 now opens NewCharacterDialog first; move 'Choose a Game System' assertion L169; ADD assertions that after clone and after character-import the new/imported sheet is shown, that a campaign-card click lands on the sheet, and that Export+Delete of the current character are reachable from an open sheet), character-management-flow.test.tsx (createCharacter helper L53; home-copy L255; same open-sheet + export/delete assertions), App.test.tsx (home-copy L101 → new landing/segment-default copy), app-sheet-error-boundary.test.tsx (home-copy L80). Update GameSystemSelector.test.tsx (ACTUAL PATH src/__tests__/components/GameSystemSelector.test.tsx — NOT the top-level path the plan implies) mocks for the dialog context; assert its three doc-drift tokens still render. _(atomic w/ siblings)_
14. Add the Phase-1 Playwright acceptance specs to e2e/ (see acceptanceGates a-h): transient-state survival across Scene↔Characters within the measured frame budget; Import+New-Character reachable with no sheet open; non-tautological scene-selection (select X in LibraryScenesView, flip to Scene, assert canvas renders X's grid, round-trip persists selectedSceneId); compound sheet-open (campaign card, post-clone, post-import all render the sheet); scene create/import-then-open lands on canvas (import selects imported[0]); Export+Delete reachable+functional from an open sheet (delete lands on closeSheet/back state); bundle/TTI (SceneManager chunk not force-loaded on Characters landing); Scene LEFT list rail gone + grid widened on first surface:'scene' render (NOT a full 'canvas dominates' assertion — that is Phase 6). _(atomic w/ siblings)_

**Test blast radius:**
- `src/__tests__/character-creation-flow.test.tsx` — Rewrite createCharacter helper (L38) to open NewCharacterDialog and click one-tap create instead of the deleted GameSystemSelector→action-bar flow; move the 'Choose a Game System' assertion (L169) to the dialog. ADD: after clone → cloned sheet shown; after character-import → imported[0] sheet shown; campaign-card character click → sheet renders; Export+Delete reachable from open sheet.
- `src/__tests__/character-management-flow.test.tsx` — Rewrite createCharacter helper (L53, current sig (_user,name)); move home-copy assertion (L255); add clone/import→sheet-shown, campaign-card→sheet, and per-card + open-sheet Export/Delete reachability assertions.
- `src/__tests__/App.test.tsx` — Replace 'Choose a Game System' assertion (L101) with the new default landing (librarySegment:'characters' with header nav) copy; assert header New Character + Import are present with no sheet open.
- `src/__tests__/app-sheet-error-boundary.test.tsx` — Replace 'Choose a Game System' assertion (L80) with the new landing copy; keep the lazy-sheet error-boundary assertions intact (sheet still renders under ErrorBoundary keyed by doc id).
- `src/__tests__/components/SceneManager.test.tsx` — Mount with the new controlled selectedSceneId + onSelectScene props; re-assert shell-owned auto-reset (stale/removed selected id → onSelectScene(scenes[0]?.id ?? null)) and that rail-click emits onSelectScene. Remove assertions that SceneManager owns selection state internally.
- `src/__tests__/scenarios/capabilityScenarios.test.tsx` — Wire the controlled five-writer seam in the scene-selection scenarios; assert create/import-then-open now selects the new/imported scene AND requests surface:'scene' via the seam (behavior relocated to LibraryScenesView).
- `src/__tests__/components/GameSystemSelector.test.tsx` — Keep alive (component is NOT deleted). Update to its new render context (inside/adjacent to NewCharacterDialog); explicitly assert the three doc-drift-guarded tokens still render (supportNotes at L164-165, supportBadgeLabels L152, supportBadgeStyles) so check:doc-drift stays green.
- `src/hooks/__tests__/useAppNav.test.ts` — NEW. openSheet(docId) yields {surface:'sheet',sheetDocId}; onSelectScene sets sceneId and (create/import path) surface:'scene'; closeSheet returns to library/characters preserving no stale sheetDocId; a @ts-expect-error case proving assertNever fails to compile if a discriminant case is dropped.
- `src/components/__tests__/NewCharacterDialog.test.tsx` — NEW. Portaled render; systemRegistry.getAll() rows present; one-click create emits openSheet with a new doc id; selectedSystem lives in the dialog.
- `src/components/__tests__/LibraryScenesView.test.tsx` — NEW. Select-only: clicking a scene emits onSelectScene(id); creating a scene (SceneCreateForm) emits onSelectScene(newId)+surface:'scene'; importing emits onSelectScene(imported[0])+surface:'scene'; renders no operating panels (distinct from the Scene canvas).
- `src/__tests__/bundle/sceneManagerChunk.test.ts` — NEW. Assert the built SceneManager chunk is a separate lazy chunk and is not part of the eager index chunk (Finding 17 regression guard).
- `src/__tests__/perf/sceneMountHide.perf.test.ts` — NEW. performance.measure on a ~30x30 grid comparing display:none vs visibility-hidden-only vs visibility+content-visibility:auto reveal-frame time; documents the chosen default (visibility-hidden-only).

**Acceptance gates:**
- npm run verify is fully green (lint incl. eslint layer-boundary, typecheck, format, coverage thresholds lines70/functions65/branches60/statements70, build, bundle-size, e2e, repo-hygiene, generated-docs, doc-drift, dead-code/knip).
- npm test passes incl. the new unit suites (useAppNav, NewCharacterDialog, LibraryScenesView) and the rewritten SceneManager/capabilityScenarios/flow/App/error-boundary/GameSystemSelector suites.
- npm run test:e2e passes the new e2e/phase1-*.spec.ts: (a) enter placement mode / type an initiative value on Scene → tab to Characters → tab back → transient state survives AND switch ≤ measured frame budget; (b) no sheet open → both 'New Character' and 'Import' reachable; (c) non-tautological scene-selection: select scene X in LibraryScenesView → flip to Scene → live canvas renders X's grid → round-trip persists selectedSceneId; (d) compound sheet-open: campaign-card click, post-clone, post-import each render the Sheet; (e) scene create/import → selected AND Scene surface renders (import selects imported[0]); (f) Export+Delete reachable+functional from an open sheet (delete lands on closeSheet/back state); (g) bundle/TTI: SceneManager chunk not force-loaded on the Characters landing; (h) Scene LEFT list rail gone + grid width increased on first surface:'scene' render (NOT 'canvas dominates').
- npm run check:doc-drift green with rules.ts/manifest.ts UNCHANGED (default path keeps GameSystemSelector + SystemStatusDashboard alive with tokens intact).
- npm run check:legal-notices green (setShowLegal path + legal strings preserved verbatim, modeled as overlay:'legal').
- tsc: the local assertNever over both surface and librarySegment discriminants compiles; a deliberately dropped case is a type error (proven by the @ts-expect-error unit test).

**CI-gate impact:**
- eslint layer-boundary: useAppNav.ts + NewCharacterDialog + LibraryScenesView + AppHeader changes are SHARED-layer — they must NOT value-import from src/systems/**; type-only imports OK. No systems→shared inversion introduced, so NO exemption edit to .eslintrc.json needed. Paired update: none, provided the boundary is respected.
- knip (dead-code): every new file is consumed in-PR (useAppNav by App; NewCharacterDialog by AppHeader/App; LibraryScenesView by the scenes segment) and each ships a test — no orphan exports. Paired update: none expected; if any new export is Playwright-only, add it to knip.json ignore in the same PR.
- hostSizeBudget (<=400 LOC per named host): Phase 1 touches NONE of the 5 named sheet hosts (dnd5e Dnd5eSheetBase.tsx + mam3e/pf2e/daggerheart/d20-legacy sheet.tsx). Unaffected. Paired update: none.
- doc-drift (RUNTIME_COPY_RULES, FILE-PATH-keyed, actually 15 entries L291-391 — see driftFound): DEFAULT Phase-1 path keeps GameSystemSelector.tsx (rule L325-327) and SystemStatusDashboard.tsx (rule L330-332) ALIVE with their supportNotes/supportBadgeLabels/supportBadgeStyles tokens rendered → rules.ts + manifest.ts UNCHANGED. Only if either guarded file is deleted/gutted (forbidden by default) must its rules.ts entry + manifest.ts + a token test be edited in the SAME commit. Relocating token rendering into NewCharacterDialog does NOT satisfy the path-keyed guard.
- coverage (70/65/60/70): each new file (useAppNav, NewCharacterDialog, LibraryScenesView) ships its own unit tests in-PR; the rewritten flow/component suites keep App+SceneManager coverage. Any DOM-only/Playwright-only module (none load-bearing in Phase 1) added to vitest.config.ts coverage exclude in the same PR. Paired update: vitest.config.ts only if a Playwright-only wrapper appears.
- bundle-size / index-chunk: SceneManager stays React.lazy() (App L30-31 untouched) and lazy-mounts on first Scene visit — no de-lazy, no offsetting-deletion pressure. New shell code is small. Paired update: add src/__tests__/bundle/sceneManagerChunk.test.ts asserting the chunk stays out of the eager index chunk (Finding 17 guard).

## Phase 2 — Structural substrate: ShellContext reducer + keepalive SurfaceStage + pause/resume-WITH-RECONCILE on the shared useEntitySync hook

**Anchors:** 12 verified, 2 drifted. **Definition of done:** "Phase 2 is done when: (1) nav state lives in src/contexts/shell-context.ts (reducer + total union + assertNever) with the provider in src/contexts/ShellContext.tsx, useAppNav.ts is a thin useContext consumer, and NO view component changed; (2) src/components/SurfaceStage.tsx lazy-mounts-then-keepalives Library/Sheet/Scene via visibility:hidden+off-screen (never display:none/content-visibility:auto), promoting the Sheet to a mounted keepalive peer, with SheetSurface hosting the ErrorBoundary+Suspense+SystemSheetRenderer block verbatim and SceneSurface carrying the Phase-1 LEFT-rail relocation forward while the RIGHT operating rail stays docked; (3) useEntitySync has an `active` knob that gates ONLY the L322-330 subscription (never the debounced push) and MANDATORILY calls sync() on active:false->true, threaded through useSync + useCampaignSync; (4) useScenes exposes reloadScenes() invoked on Scene reactivate, with the L86-98 cross-tab storage listener left UNGATED; (5) Phase-1 header tabs collapse into Library's internal segment control; (6) all new logic (reducer, active/reconcile branches) ships unit tests OR DOM-only wrappers are coverage-excluded in-PR, and `npm run verify` is green (typecheck, eslint boundary, knip, coverage, bundle, hostSizeBudget, doc-drift); (7) the two correctness Playwright gates pass: state-preserving keepalive round-trip AND reactivate-catches-up-on-missed-events (documents/campaigns via sync() reconcile, scenes via reloadScenes). INVARIANT preserved: src/scene/runtime.ts and the 12 SceneActionIntent variants are untouched; every scene gesture still emits an existing controller handler/intent and onLogToCampaign remains an App callback (not a 13th intent)."

**Drift found:**
- PATH DRIFT (highest impact): plan's Files list says `src/context/ShellContext.tsx` (singular, single file). Real dir is `src/contexts/` (PLURAL) and the AuthContext pattern it says to mirror is a TWO-FILE split — src/contexts/auth-context.ts (context+helpers, no JSX) + src/contexts/AuthContext.tsx (provider) — driven by the react-refresh only-export-components lint. Spec corrects to src/contexts/shell-context.ts + src/contexts/ShellContext.tsx. A single-file singular-dir implementation risks a lint failure and diverges from the established convention.
- ANCHOR MISLABEL: plan cites useEntitySync sync() as 'L133/L162'. sync() is DEFINED at L110-196 / RETURNED at L334; L133 is the internal fetchRemote() call and L162 is the internal onMerge(merged) call — NOT sync invocation sites. The reconcile-on-reactivate mechanism (call the returned sync callback) is intact; only the line citations are stale. Real invocation sites: L205/L303/L328.
- PREREQUISITE GAP (not drift in Phase 2's own anchors, but blocking): Phase 2 builds on Phase-1 artifacts that DO NOT EXIST in current source yet — src/hooks/useAppNav.ts, the Phase-1 SceneManager first-visit lazy-mount mechanism, the compound openSheet + compound five-writer scene-selection actions, and the header Characters/Campaigns/Scenes tabs. Phase 2 CANNOT land before Phase 1. All Phase-2-OWNED current-source anchors (useEntitySync L322-330, useSync/useCampaignSync adapters, useScenes L20/L96, the AuthContext split, App onLogToCampaign+lazy SceneManager) are verified against today's source and hold.
- MINOR: useScenes.ts exposes NO reload/refresh export today (only the L19-22 mount effect uses loadScenes). Phase 2 must ADD a reloadScenes callback to the hook's return — the plan says 'force a loadScenes() re-read' but there is no current seam for it; it is net-new surface area (small).

**Tasks (ordered):**
1. Create src/contexts/shell-context.ts (NON-component file, mirroring auth-context.ts): export the ShellNavState type ({surface:'library'|'sheet'|'scene', librarySegment:'characters'|'campaigns'|'scenes'|'content', sheetDocId:string|null, sceneId:string|null, overlay:...}), the ShellAction discriminated union (including the compound openSheet(docId) and the compound five-writer scene-selection actions), the pure shellReducer, the assertNever(x:never) exhaustiveness helper over BOTH the surface and action discriminants, and `export const ShellContext = createContext<...>(defaultValue)`. Lift the reducer/union that Phase 1 authored inside useAppNav.ts into this file verbatim — behavior-identical. No JSX here (keeps react-refresh lint green, same reason as auth-context.ts). _(atomic w/ siblings)_
2. Create src/contexts/ShellContext.tsx (component file, mirroring AuthContext.tsx): export ShellProvider({children, initialState?}) that holds useReducer(shellReducer, initial), memoizes the {state, dispatch, and named action creators openSheet/closeSheet/selectScene/setSurface/setLibrarySegment} value, and renders <ShellContext.Provider>. Import ShellContext + types from shell-context.ts. This is the ONLY file exporting a component from the pair. _(atomic w/ siblings)_
3. Repoint src/hooks/useAppNav.ts at ShellContext: replace its internal useReducer/useState with useContext(ShellContext); re-export the same public API (state discriminants + openSheet + the compound scene-selection seam) so NO view component changes (Phase 1 already made them switched islands over the total union). Assert the union is still TOTAL (assertNever compiles). _(atomic w/ siblings)_
4. Add the `active` pause/resume-with-reconcile knob to useEntitySync: (a) add `active?: boolean` (default true) to UseEntitySyncOptions (L33-37); (b) gate the subscription effect L322-330 — when active===false return undefined WITHOUT calling adapter.subscribeToRemote, and add `active` to its dependency array so flipping true re-subscribes; (c) do NOT gate pushSnapshot/debouncedPush (local edits must always persist); (d) add a reconcile effect: keep a prevActiveRef, and when active transitions false->true with userId&&isConfigured, call `void sync()` exactly once (dedupe against the initial-sync effect L203-207 via the ref so mount does not double-fire). sync's own in-flight guard (isSyncingRef/pendingResyncRef L113-119) already makes a concurrent reconcile safe. _(atomic w/ siblings)_
5. Thread `active` through src/hooks/useSync.ts: add `active?: boolean` to UseSyncOptions (L29-32) and pass it into useEntitySync({entities, onMerge, adapter, active}) at L58. Adapter object unchanged. _(atomic w/ siblings)_
6. Thread `active` through src/hooks/useCampaignSync.ts: add `active?: boolean` to UseCampaignSyncOptions (L33-36) and pass into useEntitySync(...) at L61. Adapter unchanged. _(atomic w/ siblings)_
7. Add a reactivate re-read to src/hooks/useScenes.ts: add a `reloadScenes` useCallback (setScenes(loadScenes()); setIsLoading(false)) and expose it in the return object (L158-170). Do NOT gate the L86-98 storage listener. The Scene surface calls reloadScenes() on its active:false->true transition (Finding 2) so cross-tab scene edits missed while hidden are picked up. Leave the L19-22 mount effect and all persistence untouched. _(atomic w/ siblings)_
8. Create SurfaceStage + three surface wrappers. SurfaceStage.tsx (h-dvh container) reads useAppNav().surface and lazy-mounts-then-keepalives Library/Sheet/Scene: each surface is React.lazy, mounted on FIRST visit and thereafter kept in the layout tree; the non-active surface is hidden via visibility:hidden + off-screen position (NOT display:none, NOT content-visibility:auto — Findings 7+14), generalizing Phase 1's SceneManager first-visit mechanism to all three. SheetSurface.tsx hosts the existing ErrorBoundary+Suspense+SystemSheetRenderer block VERBATIM (moved from App L~469). SceneSurface.tsx hosts the lazy SceneManager, threads onLogToCampaign + selectedSceneId + appendSceneEvent unchanged, carries the Phase-1 LEFT-rail relocation forward, keeps the RIGHT operating rail docked (Finding 20), and calls useScenes reloadScenes() on reactivate. LibrarySurface.tsx hosts the Library segment views. Each surface computes its own `active = (surface===mine)` for sync quiescence. _(atomic w/ siblings)_
9. Rewire src/App.tsx: wrap the tree in <ShellProvider>; replace the Phase-1 conditional surface render with <SurfaceStage/>; derive per-surface active flags from useAppNav().surface and pass active into useSync (L84), useCampaignSync (L146). Documents surface active = (surface==='sheet'||surface==='library'&&segment shows characters) — spec the exact mapping: documents sync active when Library OR Sheet is the visible surface, campaigns sync active when Library(campaigns) is visible, scenes reload on Scene reactivate. Keep handleLogSceneToCampaign (L132-141) as the App-owned callback threaded down through SceneSurface. Preserve the SceneManager React.lazy chunk boundary (no eager import). _(atomic w/ siblings)_
10. Collapse the Phase-1 Characters/Campaigns/Scenes header tabs into Library's INTERNAL segment control: a librarySegment flip inside LibrarySurface — no new surface, no view rewrite (the union already carries librarySegment). Header keeps only the Library/Sheet/Scene surface switcher.
11. Ship tests + coverage bookkeeping + honest re-label in the SAME PR: (a) new src/__tests__/hooks/useEntitySync.test.tsx covering the active-gate + reconcile branches (none exists today); (b) extend useSync.test.tsx/useCampaignSync.test.tsx for active pass-through; (c) shell-context reducer unit test (openSheet, compound scene-selection, assertNever exhaustiveness); (d) SurfaceStage unit test OR, if DOM-only/Playwright-only, add SurfaceStage + surfaces/** to vitest.config.ts exclude L33-50 in the same PR; (e) if any doc-drift.manifest.ts pairing is added for the new sync capability, update the paired doc — otherwise re-label the phase in docs (Steps: 'add pause/resume-with-reconcile to the shared sync engine + generalize first-visit lazy-mount', NOT 'invisible refactor'). _(atomic w/ siblings)_

**Test blast radius:**
- `src/__tests__/hooks/useEntitySync.test.tsx (NEW — no such file exists today; branches only tested transitively via useSync/useCampaignSync)` — Add: active===false skips adapter.subscribeToRemote (assert the subscribe spy is NOT called and returns no teardown); active false->true calls sync() exactly once (spy adapter.fetchRemote); active true on mount does NOT double-fire sync beyond the existing initial-sync; local edit while active===false STILL triggers debouncedPush (push not gated); reconcile is safe while a sync is in-flight (pendingResyncRef path).
- `src/__tests__/hooks/useSync.test.tsx` — Assert `active` prop is forwarded into useEntitySync and defaults to true when omitted (back-compat); existing document-sync assertions unchanged.
- `src/__tests__/hooks/useCampaignSync.test.tsx` — Same as useSync: assert `active` forwards and defaults true.
- `src/__tests__/contexts/shellReducer.test.ts (NEW)` — Cover shellReducer: openSheet(docId)->{surface:'sheet',sheetDocId}; the compound five-writer scene-selection action sets sceneId AND surface:'scene'; closeSheet/back maps the three setCurrentDocId(null) sites; assertNever exhaustiveness compiles for both discriminants. This is the coverage counterweight for logic MOVED OUT of coverage-excluded App.tsx into instrumented shell-context.ts.
- `src/__tests__/components/SurfaceStage.test.tsx (NEW or Playwright-only)` — Assert first-visit lazy-mount then keepalive: switching surfaces does not unmount the previously-visited surface; hidden surface uses visibility:hidden (not display:none); SceneManager chunk is not force-loaded until first Scene visit. If DOM-measurement-only, exclude from coverage in the same PR instead of asserting in vitest.
- `src/__tests__/App.test.tsx` — Update for the SurfaceStage swap + ShellProvider wrap: surface switching now keepalives rather than remounts; assert Library->Scene->Sheet round-trip preserves scroll/filter/selection and no remount flash. App.tsx itself stays coverage-excluded (vitest L48), so these are behavioral render(<App/>) assertions.
- `src/__tests__/components/SceneManager.test.tsx` — SceneManager now mounts inside SceneSurface (keepalive) rather than App's conditional Suspense; re-assert onLogToCampaign + selectedSceneId + appendSceneEvent props thread through unchanged and reloadScenes fires on reactivate.

**Acceptance gates:**
- npm run typecheck — assertNever over BOTH ShellNavState discriminants compiles; useEntitySync/useSync/useCampaignSync `active` types propagate; no `any`.
- npm test -- useEntitySync useSync useCampaignSync shellReducer — new active-gate/reconcile + reducer branches pass; existing sync suites green with active defaulting true.
- npm run test:coverage — branches>=60 / functions>=65 / lines>=70 / statements>=70 still met AFTER logic moves from coverage-excluded App.tsx into instrumented shell-context.ts + useEntitySync (the reducer + active branches carry their own tests, or the DOM-only SurfaceStage/surfaces are in the vitest exclude list).
- Playwright: enter placement mode / type an initiative value on Scene, tab to Library, tab to Sheet, tab back to Scene — assert transient Scene state survives (keepalive, no remount) and each surface keeps its scroll/filter/selection position.
- Playwright (quiescence correctness): with two tabs signed in, mutate a document/campaign in tab B while tab A's surface is hidden (quiesced), return to that surface in tab A, assert the reactivated surface immediately shows tab B's change (sync() reconcile ran) — no stale data.
- Playwright (scene cross-tab): edit a scene in tab B while tab A is on a non-Scene surface, return to Scene in tab A, assert reloadScenes() surfaced the edit (the L86-98 storage listener was NOT gated).
- npm run verify — eslint layer-boundary green (ShellContext/SurfaceStage/surfaces are shared-layer and value-import NO src/systems/**; SheetSurface renders the already-shared SystemSheetRenderer only); knip green (all new files reachable from App/ShellProvider); bundle-size + hostSizeBudget unaffected (no host touched); doc-drift green (no guarded file touched).

**CI-gate impact:**
- eslint layer-boundary: NEW shared-layer files (src/contexts/shell-context.ts, ShellContext.tsx, src/components/SurfaceStage.tsx, src/components/surfaces/**) must NOT value-import src/systems/**. Verified current hooks are clean (grep empty). SheetSurface may render SystemSheetRenderer (already shared) and type-only-import system types. Paired update: none needed if boundary respected.
- knip dead-code: NEW files (shell-context, ShellContext, SurfaceStage, 3 surface wrappers) must be reachable. Reachability chain: App -> ShellProvider -> SurfaceStage -> surfaces; useAppNav -> ShellContext. Paired update: ensure App.tsx actually renders SurfaceStage in the same PR or knip flags them.
- coverage thresholds (branches60/functions65/lines70/statements70, vitest.config.ts L51-56): PRIMARY gate risk. Logic moving out of coverage-EXCLUDED App.tsx (L48) into INSTRUMENTED shell-context.ts + useEntitySync raises measured surface area. Paired update: ship shellReducer.test.ts + useEntitySync.test.tsx (active/reconcile) in-PR; add DOM-only SurfaceStage/surfaces to vitest exclude (L33-50) if not unit-tested.
- hostSizeBudget (5 hosts, 400 LOC, hostSizeBudget.test.ts): NO impact — Phase 2 edits none of the 5 named sheet hosts. No paired update.
- doc-drift (RUNTIME_COPY_RULES, file-path-keyed, docs/doc-drift.rules.ts + manifest.ts): NO impact — grep confirms no Phase-2 file is guarded or manifest-paired. No rules.ts/manifest.ts edit required. ONLY action: the honest phase re-label (Step 11) in the design doc, and IF a doc-drift manifest pairing is later added for the new sync-engine behavior, update its paired doc.
- bundle-size: keepalive adds shell DOM but no new eager code IF SurfaceStage keeps each surface (esp. SceneManager) on React.lazy first-visit mounting. Paired update: assert SceneManager chunk stays out of the eager index chunk (carry Phase-1's bundle/TTI assertion forward).

**Open questions:**
- Exact per-surface `active` mapping for documents sync: documents back BOTH the Sheet surface and the Library 'characters' segment, so documents-sync should be active when EITHER is visible (proposed: active = surface==='sheet' || (surface==='library' && librarySegment==='characters')). Campaigns-sync active only for Library 'campaigns'. Confirm this mapping with the owner before wiring App L84/L146, since over-aggressive quiescing could pause sync for a surface that still displays that data.
- Whether SurfaceStage/surface wrappers get real vitest unit tests or go on the coverage-exclude list — decides whether the branch/function thresholds need the reducer+useEntitySync tests to carry ALL the new instrumented surface area. Recommend unit-testing the reducer + useEntitySync active branches (pure/logic) and excluding only the DOM-measurement SurfaceStage.
- PWA device testing for keepalive baseline DOM/memory + mobile dvh + virtual-keyboard insets is called out as a risk but is not a CI gate — needs a manual device pass; flag whether that blocks the phase or is tracked separately.

## Phase 3 — Shared Dock (5 tabs incl. a party tab from App `documents`) + SheetDispatchContext (inverted control) + delete the read-only bestiary tab. Invariant preserved: src/scene/runtime.ts and the 12 SceneActionIntent variants are untouched (party/monster drag verbs deferred to Phase 4; Phase 3 dock is click-select + browse-only).

**Anchors:** 13 verified, 4 drifted. **Definition of done:** "One summonable Dock built on Tabs.tsx stands up with FIVE typed tabs (party + monster/spell/feat/equipment), reachable identically from Library, Sheet, and Scene via the header-rail summon control. The spell/feat/equipment tabs click-add into the current sheet through the new inverted-control src/contexts/SheetDispatchContext.tsx (the systems sheet registers its existing add-handlers UP; the shared dock dispatches), hard-gated on a RESOLVED active-document id (disabled, never silently targeting a stale controller). The PARTY tab is browse-only, sourced from the App documents prop (not useDockResources, no click-add verb, no drag) — the load-bearing character-document source Phase 4's 1-choice gate needs. The four SRD tabs load via a new shared useDockResources keyed by an explicit active-system selector. The read-only bestiary is fully removed: Dnd5eMonsterBrowserTab.tsx deleted and monster plumbing unthreaded from all four files (Dnd5eSheetBase, useDnd5eSheetResources, useDnd5eSheetController, Dnd5eTabsNavigation), with the shared MonsterBrowser re-hosted in the dock. All new modules ship in-PR tests; the SystemSheets and useDnd5eSheetResources suites are rewritten. src/scene/runtime.ts and the 12 SceneActionIntent variants are untouched (party/monster drag-to-scene verbs are deferred to Phase 4). npm run verify is green with every CI-gate impact paired: eslint boundary clean, knip clean, hostSizeBudget re-measured (<=400), doc-drift green with NO paired edit needed for the monster delete, coverage thresholds met, bundle-size unchanged."

**Drift found:**
- Context directory: the plan's src/context/SheetDispatchContext.tsx path is wrong — there is no src/context/ (singular). The repo convention is src/contexts/ (plural), holding the AuthContext split (AuthContext.tsx + auth-context.ts). Correct path: src/contexts/SheetDispatchContext.tsx. (Same drift affects Phase 2's src/context/ShellContext.tsx.)
- Monster-tab delete is broader than the plan's 2 files. The plan names only Dnd5eMonsterBrowserTab.tsx (delete) + Dnd5eTabsNavigation.tsx (drop trigger). Monster plumbing actually threads FOUR files: also useDnd5eSheetResources.ts (monsters deferred resource L105-114, warmMonsterBrowser L227-232, import L20, returns) and useDnd5eSheetController.ts (monstersLoaded L53/L215, warmMonsterBrowser L61/L235). Missing any breaks typecheck/knip.
- Monster tab is doc-drift-SAFE. The plan's Files list conditionally names docs/doc-drift.rules.ts + manifest.ts 'only if the deleted tab is guarded.' Verified condition is FALSE: Dnd5eMonsterBrowserTab.tsx is absent from RUNTIME_COPY_RULES (full table L291-391) and from manifest.ts. No paired doc-drift edit is required for this delete — the 'check before deleting' step resolves to a no-op.
- Anchor line drift in doc-drift.rules.ts: the plan cites RUNTIME_COPY_RULES at 'L295-381' and the feat browserSupport pin at 'L355.' Current: the table spans L291-391 (15 entries); the feat pin is the entry at path L353 with token 'DND5E_FEAT_COPY.browserSupport' at L355. Close enough that the pin still holds, but the range moved.
- Dnd5eTabsNavigation 'monsters' TabsTrigger line was uncited by the plan; it is at L87-95, with the paired onWarmMonsters prop at interface L22 / destructure L32 and the Skull icon import at L6 — all three removed together.
- The four shared browsers already expose optional onSelect callbacks (onSelectMonster/Spell/Feat/Equipment), so the dock click-add wiring reuses existing props rather than adding new ones — a positive confirmation, not a blocker.

**Tasks (ordered):**
1. Create src/contexts/SheetDispatchContext.tsx (CORRECTED path — plural, matching AuthContext convention; NOT the plan's src/context/). Shared-layer inverted-control registry: a Provider holds { activeDocId: string | null, handlers: { addSpell?, addFeat?, addEquipment? } } in a ref+state; export useSheetDispatchRegister() for the active sheet to publish its handlers + active-doc id UP, and useSheetDispatch() for the dock to read handlers + a derived canDispatch boolean = Boolean(activeDocId && handler). No value-import from src/systems/**. _(atomic w/ siblings)_
2. Create src/dock/dockRegistry.tsx — a typed discriminated union of the FIVE tab descriptors (kind: 'party'|'monster'|'spell'|'feat'|'equipment') each with id/label/icon; party+monster are browse-only in Phase 3, spell/feat/equipment carry the click-add verb. _(atomic w/ siblings)_
3. Create src/dock/useDockResources.ts — a NEW shared loader keyed by an explicit active-system selector (system id passed in, NOT read from a sheet controller), calling the shared dataLoader loaders (loadMonstersForSystem/loadSpellsForSystem/loadFeatsForSystem/loadEquipmentForSystem) for the FOUR SRD tabs only. Must NOT import useDnd5eSheetResources or anything under src/systems/**. The party tab bypasses this loader entirely. _(atomic w/ siblings)_
4. Create src/dock/DockProvider.tsx — summon state (open/closed + active tab id) and receives the App `documents` prop (CharacterDocument[]) for the party tab; provides dock context. Mount it at App/shell level so the dock is summonable identically from Library/Sheet/Scene surfaces. _(atomic w/ siblings)_
5. Create src/dock/Dock.tsx built on Tabs.tsx (TabsList/TabsTrigger/TabsContent). Render five TabsContent bodies. Re-host the shared browsers VERBATIM: SpellBrowser/FeatBrowser/EquipmentBrowser wire their onSelect* to SheetDispatchContext dispatch, DISABLED (grayed, no dispatch) when canDispatch is false; MonsterBrowser is browse-only (drag verb is Phase 4). Keep the browsers lazy (they already use lazyWithPreload) so the dock does not bloat the index chunk. _(atomic w/ siblings)_
6. Create src/dock/PartyDockTab.tsx — a BROWSE-ONLY roster tab rendering the DockProvider `documents` list (mirrors the roster SceneManager backs at L529). NO click-to-add-to-sheet verb (you do not add a character to a character); NO drag (drag-to-scene source is wired Phase 4). Does NOT go through useDockResources. This is the load-bearing character-document source the Phase-4 1-choice gate depends on. _(atomic w/ siblings)_
7. Wire the active dnd5e SheetSurface to PUBLISH its add-handlers UP via useSheetDispatchRegister: register controller.handleEquipmentSelect (equipment), the spell add path (onSelectSpell), the feat add path (onSelectFeat), and the resolved active-doc id, in a 1-2 line block inside Dnd5eSheetBase.tsx. Measure host LOC after (must stay <=400; expected ~300). _(atomic w/ siblings)_
8. Pre-delete doc-drift CHECK (mandatory per plan): confirm Dnd5eMonsterBrowserTab.tsx is absent from BOTH docs/doc-drift.rules.ts RUNTIME_COPY_RULES and docs/doc-drift.manifest.ts (verified absent). Result: NO rules.ts/manifest.ts edit for this delete. Do NOT touch the feat browserSupport pin (rules.ts L353-355 — that is Phase 5).
9. DELETE the read-only bestiary tab and unthread ALL monster plumbing (4 files): delete src/systems/dnd5e/shared/components/Dnd5eMonsterBrowserTab.tsx; in Dnd5eSheetBase.tsx remove import L19, onWarmMonsters L137, render L275-277; in useDnd5eSheetResources.ts remove Dnd5eMonsterBrowserTab import L20, the monsters deferred resource L105-114, warmMonsterBrowser L227-232, the monsters/monstersLoaded/warmMonsterBrowser return keys, and loadMonstersForSystem from the L12 import if now unused; in useDnd5eSheetController.ts remove monstersLoaded (L53/L215) and warmMonsterBrowser (L61/L235); in Dnd5eTabsNavigation.tsx remove the 'monsters' TabsTrigger L87-95, the onWarmMonsters prop (interface L22 + destructure L32), and the Skull icon import L6. _(atomic w/ siblings)_
10. Ship in-PR unit tests for every new module and rewrite the blast-radius suites (see testBlastRadius). Confirm src/dock/** and src/contexts/SheetDispatchContext.tsx contain zero value-imports of src/systems/** (eslint boundary). _(atomic w/ siblings)_

**Test blast radius:**
- `src/__tests__/components/SystemSheets.test.tsx` — L656/L969/L1016 spy on loadMonstersForSystem and exercise the dnd5e sheet Monsters tab. Remove the monster-tab render/warm assertions; ADD an assertion that the dnd5e sheet tab strip no longer contains a 'Monsters' TabsTrigger (queryByRole('tab',{name:/monsters/i}) is null).
- `src/__tests__/systems/dnd5e/useDnd5eSheetResources.test.tsx` — Mocks loadMonstersForSystem (L20). Drop the monsters deferred-resource / monstersLoaded / warmMonsterBrowser assertions and remove the loadMonstersForSystem mock if the hook no longer references it.
- `src/__tests__/hostSizeBudget.test.ts` — No assertion change (Dnd5eSheetBase LOC drops from 301 after the monster render removal minus the +1-2 registration line). Re-run to confirm host #1 still <=400; add nothing — this is a passive guard.
- `src/__tests__/scenarios/capabilityScenarios.test.tsx` — If any scenario asserts a monster tab is reachable from the sheet, repoint the browse expectation to the dock; otherwise unaffected.
- `src/__tests__/components/MonsterBrowser.test.tsx` — Component is reused (re-hosted in the dock), so existing assertions stand. Optionally add a smoke test that MonsterBrowser renders inside the Dock browse tab.
- `src/dock/__tests__ (new)` — New specs: DockProvider (summon open/close, documents threading), Dock (renders 5 tabs; spell/feat/equipment click-add dispatches via SheetDispatchContext), useDockResources (loads the 4 SRD catalogs by explicit system selector, party bypasses it), dockRegistry (5 typed descriptors).
- `src/dock/__tests__/PartyDockTab.test.tsx (new)` — Assert it renders the roster from the documents prop, has NO add-to-sheet control, and has NO draggable affordance in Phase 3 (drag arrives Phase 4).
- `src/contexts/__tests__/SheetDispatchContext.test.tsx (new)` — Assert a registered handler is invoked on dispatch when activeDocId resolves; assert canDispatch is false (and dispatch is a no-op / control disabled) when no active doc — never silently targets a stale/last-mounted controller.

**Acceptance gates:**
- npm run verify is green (eslint layer-boundary, knip, hostSizeBudget, doc-drift, coverage thresholds lines70/functions65/branches60/statements70, bundle-size).
- npm test passes with the rewritten SystemSheets/useDnd5eSheetResources suites and the new dock/party/context specs.
- RTL/Playwright: the Dock is summonable from all three surfaces (Library, Sheet, Scene) via the header-rail dock-summon control and renders exactly 5 tabs (party, monster, spell, feat, equipment).
- RTL/Playwright: with a sheet open, open the dock Spell tab, click-add a spell, assert it is added to the active character (dnd5e handler fires) and the sheet state updates; repeat for feat and equipment.
- RTL: with NO sheet open (surface !== 'sheet' or no active doc), the dock spell/feat/equipment click-add controls are DISABLED and dispatch is a no-op (active-doc gate; no stale-controller dispatch).
- RTL: the Party tab renders the roster from App documents and exposes NO add-to-sheet verb and NO drag handle in Phase 3.
- Playwright/RTL: the dnd5e character sheet tab strip no longer shows a 'Monsters' tab; MonsterBrowser is reachable only via the dock.
- Static: rg over src/dock/** and src/contexts/SheetDispatchContext.tsx finds zero value-imports of src/systems/** (type-only allowed).
- runtime.ts and the 12 SceneActionIntent variants are byte-for-byte unchanged (git diff empty for src/scene/runtime.ts).

**CI-gate impact:**
- eslint layer-boundary: src/dock/** and src/contexts/SheetDispatchContext.tsx are shared layer — they MUST NOT value-import src/systems/**. Inversion satisfies this: the sheet (systems) imports the shared context and registers UP; the dock reads from the context. useDockResources imports only shared dataLoader loaders, never useDnd5eSheetResources. Paired update: keep any systems-type references type-only.
- knip: deleting Dnd5eMonsterBrowserTab.tsx + removing monsters/monstersLoaded/warmMonsterBrowser from useDnd5eSheetResources.ts and useDnd5eSheetController.ts must leave no orphaned export; loadMonstersForSystem STAYS live (useSceneEncounter/systemCatalog/scripts/new useDockResources). Paired update: every new src/dock/* module must be imported into the App/shell wiring or knip flags it as unused.
- hostSizeBudget: Dnd5eSheetBase.tsx (host #1, 400-LOC ceiling) nets DOWN from 301 (remove monster import+wire+render, add ~1-2 line dispatch registration). Paired update: re-measure in the PR checklist; no other named host changes.
- doc-drift: the monster tab is NOT pinned in docs/doc-drift.rules.ts RUNTIME_COPY_RULES nor docs/doc-drift.manifest.ts — so this delete needs NO paired rules.ts/manifest.ts edit (contrast Phase 5, which EDITS six guarded inline files). Do NOT edit the feat browserSupport pin (rules.ts L353-355). Paired update: none — but run check:doc-drift to confirm green.
- coverage (lines70/functions65/branches60/statements70): new dock/loader/party/context branches ship their own tests in-PR; if any DOM-only piece is only Playwright-exercisable, add it to the vitest.config.ts coverage exclude in the SAME PR.
- bundle-size: the dock re-hosts existing browsers already split via lazyWithPreload — keep them lazy so re-hosting adds no eager index-chunk weight. Paired update: verify the browsers remain in their own chunks, not pulled into the dock's eager entry.

## Phase 4 — Drag keystone (pointer-events engine) + zero-arg bound emit(intent) scene seam + party-tab character-document drag source + TWO-part prototype GATE (3b-i party 1-choice auto-apply; 3b-ii monster 2+ inline-chip) + dynamic-viewport probe + PlacementMode mutual-exclusion.

**Anchors:** 15 verified, 2 drifted. **Definition of done:** "Phase 4 is done when: (1) a greenfield pointer-events engine (setPointerCapture, scroll-vs-drag disambiguation, portaled ref-transform DragLayer, no per-frame React state) ships with passing pointerEngine + useDropTarget unit tests; (2) a zero-arg emit(intent):boolean seam is published by the mounted SceneSurface, bound to the shell-resolved selectedScene via SceneManager's existing emitSceneAction (L356), with runtime.ts and all 12 SceneActionIntent variants byte-unchanged; (3) the Phase-3 party dock tab is a working character-document drag source whose {documentId} payload SceneSurface resolves against the documents prop (mirror L529); (4) 3b-i (party character) fires place-token auto-apply with NO menu, and 3b-ii (bestiary monster) resolves allegiance through a minimal ConfirmDialog-plumbing friendly/hostile chip then emits place-token/set-token-allegiance; (5) an interim SceneGridView drop target ships user-visibly (drop resolved via elementFromPoint over added data-coords, routed to the unchanged onCellActivate contract) sourced from the party + monster tabs; (6) under VITE_SCENE_DRAG_ENABLED the token (L1030/L1032) and adversary (L1064/L1068) PlacementMode entries are hidden so exactly one affordance exists per covered kind, the marker toggle (L1146) untouched, and the machine itself NOT deleted; (7) the fund/kill GATE is evaluated on a transformed ~900-cell spike with an explicit post-drop reconcile budget for BOTH sub-gates PLUS a dynamic-viewport probe (variable scale+translate inversion, wheel-zoom-to-cursor, continuous-repaint budget, touch pinch/pan) that de-risks Phase-6 5d; and (8) npm run verify is green with no doc-drift or hostSizeBudget pairing needed (Phase 4 touches no guarded path or named host), the emit seam's DragLayer consumer landing in the same PR to satisfy knip, and pointerEngine/chip/party-source tests satisfying coverage."

**Drift found:**
- INTERIM DROP MATH (task 6): the plan's 'floor((clientX-rect.left)/cellPx) on the untransformed grid' is NOT implementable as written — SceneGridView.tsx computes per-cell `position` and wires onClick={()=>onCellActivate?.(position)} (L128) with no grid rect and no exposed cellPx. Correction: add a container ref + data-x/data-y (or cellPx-derived rect) and resolve drops via document.elementFromPoint -> the cell's SceneCoordinate, then call the existing onCellActivate(position). Small real code addition, same elementFromPoint hit-test the 3a spike exercises.
- CONFIRMDIALOG REUSE (task 8): ConfirmDialog.tsx is a strict TWO-action (confirm/cancel) portaled dialog (props L6-15). The friendly/hostile chip needs three outcomes (Friendly / Hostile / dismiss=snap-back). Correction: reuse ConfirmDialog's portal+inert+focus-trap+Escape PLUMBING in a thin sibling chooser; do NOT semantically overload onConfirm=Friendly/onCancel=Hostile (cancel conventionally aborts). Phase 6 relocates/hardens it.
- PREREQUISITE SEQUENCING (task 1): src/dock/** (Phase 3 Dock + PartyDockTab), src/components/surfaces/SceneSurface.tsx (Phase 2), and any SceneDispatchContext do NOT exist at current HEAD. Phase 4 is hard-blocked on Phases 2-3. The party dock tab (the load-bearing 1-choice drag SOURCE, Finding 15) and the SceneSurface publish point must exist before Phase 4 can wire them. Not source drift, but a build-order prerequisite the spec must enforce.
- buildPlacedToken SPAN: plan cites tokenPlacement.ts:36-68; the function actually spans L36-70 (body closes L70). The cited kind/allegiance logic (L43, L67-68) is exact. Immaterial to behavior; noted for precise line references.
- useSceneEncounter 'share ONE path' (task 8): useSceneEncounter.ts owns the encounter DRAFT + monster catalog but does NOT emit place-token today (placement is SceneManager.handleCellActivate -> emitSceneAction). The fold means reusing its monster catalog (monstersById) for statblock resolution, NOT rerouting the emit through it. Scoped to catalog reuse to avoid duplicating a loader.
- No CI-gate drift: contrary to the general Phase-1/3/5 pattern, Phase 4 requires NO doc-drift rules.ts/manifest.ts pairing (touches none of the 13 guarded paths, verified L307-379) and NO hostSizeBudget concern (SceneManager is not one of the 5 named hosts). Flagged so reviewers do not demand a phantom pairing.

**Tasks (ordered):**
1. PREREQUISITE GATE: confirm Phases 1-3 are merged (src/hooks/useAppNav.ts total union + lifted selectedSceneId; src/components/surfaces/SceneSurface.tsx keepalive; src/dock/{DockProvider,Dock,PartyDockTab,useDockResources,dockRegistry} with the browse-only party tab). If any is absent, Phase 4 cannot build — these are its drag source and publish points. Verify PartyDockTab renders the App documents roster and SceneSurface hosts the Scene subtree. _(atomic w/ siblings)_
2. Add a single feature-flag predicate isSceneDragEnabled() reading `import.meta.env.VITE_SCENE_DRAG_ENABLED === 'true'`, mirroring src/ai/gatewayClient.ts:24 isAiEnabled(). This ONE predicate gates every 3b-i/3b-ii drag mount AND the paired PlacementMode entry-point hiding (Finding 21), so the two affordances are never simultaneously visible in any flag state.
3. Build the greenfield pointer engine: pointerEngine.ts (pointerdown->setPointerCapture, scroll-vs-drag disambiguation via activation delay + movement tolerance for touch, drag lifecycle state, emits start/move/end with clientX/clientY), useDropTarget.ts (registers a drop zone element + a resolver(clientX,clientY)->payload-or-null via document.elementFromPoint hit-test), DragProvider.tsx (context holding active drag payload + registered drop targets), DragLayer.tsx (portaled ghost positioned by DIRECT ref transform, pointer-events:none, NO per-frame React state). No DnD library. Ship pointerEngine + useDropTarget UNIT tests in the same PR; add DragLayer.tsx to the vitest coverage exclude in the SAME PR if only Playwright-exercisable.
4. Expose the zero-arg bound emit seam: create SceneDispatchContext.tsx. The mounted SceneSurface publishes emit(intent: SceneActionIntent)=>boolean bound to `(intent)=>emitSceneAction(selectedScene, intent)` (SceneManager L356 + the resolved selectedScene). runtime.ts and the 12 intents are untouched. Hard-guard: when no scene is resolved, emit is a no-op returning false (never targets a stale scene). Buildable with no orphan export because the DragLayer (Task 6/7) is its first consumer. _(atomic w/ siblings)_
5. Wire PartyDockTab as the character-document drag SOURCE (Finding 15): make each roster entry draggable via the pointer engine; the drag payload carries {documentId}. SceneSurface resolves documentId against the shell-threaded documents prop mirroring SceneManager L529 (documents.find(d=>d.id===documentId)) so buildPlacedToken receives its linkedDoc. This is the ONLY character-document drag source in Phases 1-4. _(atomic w/ siblings)_
6. Add the interim SHIPPED drop target on SceneGridView (DRIFT-corrected): SceneGridView has no grid rect/cellPx and computes per-cell `position` (L128). Add a grid container ref + data-x/data-y on each cell (or a cellPx-derived rect) so useDropTarget's resolver maps clientX/clientY -> the cell's SceneCoordinate via document.elementFromPoint, then routes to the existing onCellActivate(position) contract. Source drops from the party tab (character) and monster dock tab (monster). This is real, user-visible now on the untransformed grid. _(atomic w/ siblings)_
7. 3b-i GATE (genuine 1-choice auto-apply): dragging a PARTY character-document token resolves linkedDoc with non-npc kind -> buildPlacedToken returns kind='character', playerControlled:true, NO allegiance (tokenPlacement L43/L67). On drop, emit({type:'place-token', token}) via the zero-arg seam with NO menu. Illegal drops surface runtime.ts's existing `issues` as a toast + snap-back (reuse setActionIssues path around SceneManager L382). _(atomic w/ siblings)_
8. 3b-ii GATE (2+ inline-chip classifier): make a BESTIARY MONSTER dock item draggable. A statblock NPC requires tokenAllegiance (buildPlacedToken L68), so pull a MINIMAL friendly/hostile chip FORWARD — a thin sibling reusing ConfirmDialog's portal/inert/focus-trap plumbing (ConfirmDialog is 2-action; the chip needs Friendly/Hostile/dismiss, so do NOT overload onConfirm/onCancel as allegiance — build a minimal 2-choice-plus-dismiss chooser). On choice, emit place-token with the resolved allegiance (or place-token then set-token-allegiance). Reuse useSceneEncounter's monster catalog (monstersById) for the statblock — no new loader. Phase 6 relocates/hardens this chip. _(atomic w/ siblings)_
9. PlacementMode mutual-exclusion (Finding 21): under isSceneDragEnabled(), hide the token PlacementMode entry (TokenPanel isPlacing L1030 / onTogglePlace L1032) for the character kind and the adversary entry (L1064 variant / L1068 setter) for the monster/npc kind, so exactly one affordance exists per covered kind. When the flag is OFF, PlacementMode stays the sole affordance. Marker toggle (L1146/L1148) is UNTOUCHED — markers are not drag-covered until Phase 6. Do NOT delete the PlacementMode machine (that is Phase 6 5b). _(atomic w/ siblings)_
10. 3a SPIKE harness — transformed ~900-cell surface (STATIC retire + DYNAMIC probe, Findings 3+10): build a spike harness (test/dev-only, NOT shipped in the index chunk) rendering ~900 memoized cells inside a CSS transform (scale+translate). Prove (static) clientX/clientY -> integer SceneCoordinate inversion AND elementFromPoint hit-testing under active setPointerCapture. EXTEND (dynamic) to variable scale+translate inversion floor((clientX-rect.left-tx)/(cellPx*scale)) with wheel-zoom-to-cursor anchoring + clamping, a CONTINUOUS pan/zoom repaint frame budget at ~900 transform-positioned cells, and touch pinch-zoom + one-finger-pan arbitration. Instrument post-drop reconcile frame time. This PROBES (does not retire) the Phase-6 5d viewport subsystem.
11. THE GATE evaluation (Finding 3): run BOTH sub-gates against the transformed ~900-cell spike with an EXPLICIT post-drop reconcile frame-time budget — NOT the trivial untransformed grid, NOT a hardcoded default allegiance faking a 1-choice monster. Record the measured 3b-i (party 1-choice) and 3b-ii (monster chip) reconcile times + the dynamic-viewport continuous-repaint budget. If either sub-gate misses the feel/frame budget, or the viewport probe shows continuous-repaint is unreachable, the interaction thesis (or specifically the pan/zoom subsystem) is falsified BEFORE Phases 5-6 fund the expensive work.

**Test blast radius:**
- `src/__tests__/drag/pointerEngine.test.ts (new)` — Unit-test scroll-vs-drag disambiguation (activation delay + movement tolerance), setPointerCapture lifecycle, start/move/end payload emission. Branch-heavy engine — required for the coverage.branches:60 gate. Assert a below-tolerance move does NOT start a drag and an above-tolerance move does.
- `src/__tests__/drag/useDropTarget.test.ts (new)` — Assert resolver(clientX,clientY) returns the correct cell payload via mocked document.elementFromPoint over a data-coords grid, and returns null outside any drop zone.
- `src/__tests__/drag/partyDragSource.test.tsx (new)` — Assert PartyDockTab entries emit a drag payload carrying documentId, and SceneSurface resolves it to a linkedDoc via documents.find (mirror L529).
- `src/__tests__/scene/sceneDispatchContext.test.tsx (new)` — Assert emit(intent) calls emitSceneAction with the resolved selectedScene and returns its boolean; assert emit is a no-op returning false when no scene is resolved (never targets a stale scene).
- `src/__tests__/drag/dynamicViewportProbe.test.tsx (new)` — Assert integer coordinate inversion under fixed 1:1 AND variable scale+translate (floor((clientX-rect.left-tx)/(cellPx*scale))), wheel-zoom-to-cursor anchoring, and clamping. This is the Finding-10 probe that de-risks Phase-6 5d.
- `src/__tests__/drag/gateBudget.test.tsx (new)` — Record + assert post-drop reconcile frame budget for BOTH sub-gates on the transformed ~900-cell spike; assert the 1-choice party path emits place-token with NO chip render and the monster path renders the friendly/hostile chip before emitting.
- `src/__tests__/components/SceneManager.test.tsx` — Add cases: with VITE_SCENE_DRAG_ENABLED on, the token PlacementMode toggle (L1030/L1032) and adversary toggle (L1064/L1068) are hidden while the marker toggle (L1146) remains; with flag off, all three PlacementMode entries render. Assert emitSceneAction still fires place-token via the existing handleCellActivate path (regression). Assert SceneManager publishes the bound emit.
- `src/__tests__/scenarios/capabilityScenarios.test.tsx` — Mounts SceneManager directly — re-assert scene-token placement still works via both the interim grid drop and legacy PlacementMode (flag off), so the mutual-exclusion gating does not break the existing capability scenario.
- `e2e/scene-drag.spec.ts (new Playwright)` — Acceptance: drag a party character token onto the interim grid -> token appears at the released cell with NO menu (3b-i auto-apply); drag a monster -> friendly/hostile chip appears, choose Hostile -> token lands with allegiance (3b-ii); with the flag on, assert the token/adversary Place buttons are absent and only one affordance per kind exists; illegal drop -> toast + snap-back.

**Acceptance gates:**
- npm run test -- src/__tests__/drag src/__tests__/scene — pointerEngine, useDropTarget, party drag source, SceneDispatchContext, dynamic-viewport probe, and gate-budget suites all pass.
- npm run test:e2e -- e2e/scene-drag.spec.ts — 3b-i party character drops with NO menu (auto-apply); 3b-ii monster drop shows friendly/hostile chip then lands with allegiance; illegal drop shows toast + snap-back; under the flag exactly one placement affordance exists per covered kind.
- Gate-budget assertion: both sub-gates' post-drop reconcile frame time on the transformed ~900-cell spike are under the explicit budget, and the dynamic-viewport continuous-repaint probe meets its frame budget — recorded, not on the trivial untransformed grid.
- npm run verify — full CI gate green: eslint layer-boundary (drag/context/dock are shared layers and MUST NOT value-import src/systems/**; SceneDispatchContext inverts control), knip (no orphan exports — the emit seam has DragLayer as its consumer), coverage thresholds (lines70/functions65/branches60/statements70) met with pointerEngine tests + DragLayer added to coverage.exclude if DOM-only, hostSizeBudget (unaffected — SceneManager not a named host), doc-drift (unaffected — no guarded path touched), bundle-size (spike harness dev/test-only, NOT in the eager index chunk).
- grep confirms src/scene/runtime.ts and the 12 SceneActionIntent variants (scene.ts L261-288) are byte-identical to HEAD — no gesture added a 13th intent; every drop emits place-token / set-token-allegiance via the bound emit seam.
- Flag-off regression: with VITE_SCENE_DRAG_ENABLED unset, SceneManager renders all three PlacementMode toggles (token L1030, adversary L1064, marker L1146) and handleCellActivate placement is unchanged.

**CI-gate impact:**
- eslint layer-boundary: src/components/drag/**, src/context/SceneDispatchContext.tsx, and src/dock/** are SHARED layers — they must NOT value-import from src/systems/**. The emit seam carries only SceneActionIntent (a src/types type) and the payload carries a documentId string, so no systems import is needed. No .eslintrc.json exemption required. PAIRED UPDATE: none.
- knip (dead-code): the zero-arg emit(intent) export must have its consumer (DragLayer / SceneSurface drop handler) land in the SAME PR, or knip flags an orphan export — this is exactly why the plan schedules the seam in Phase 4 WITH its DragLayer consumer, not Phase 3. PAIRED UPDATE: land seam + consumer together; no knip.json change needed.
- coverage thresholds (lines70/functions65/branches60/statements70): the branch-heavy pointerEngine + the pulled-forward chip + party drag-source MUST ship unit tests in-PR. PAIRED UPDATE: if DragLayer.tsx is only Playwright-exercisable (DOM/rAF ghost), add it to vitest.config.ts coverage.exclude (L33-46) in the SAME PR.
- doc-drift (RUNTIME_COPY_RULES, 13 file-path-keyed paths, docs/doc-drift.rules.ts L307-379): Phase 4 touches NONE of the guarded paths (it edits SceneManager, SceneGridView, ConfirmDialog, useSceneEncounter, Dock, PartyDockTab + new files). PAIRED UPDATE: NONE — unlike Phases 1/3/5, no rules.ts+manifest.ts pairing is required. Explicitly assert this in the PR checklist so a reviewer does not demand a phantom pairing.
- hostSizeBudget (<=400 LOC per named host): the 5 named hosts are sheet hosts; SceneManager.tsx (1207 LOC) is NOT one, so the flag-gate + bound-emit edits do not trip it. PAIRED UPDATE: none. (SceneManager decomposition is Phase 6.)
- bundle-size / TTI: the ~900-cell transformed spike harness and probe are dev/test-only and MUST NOT enter the eager index chunk. PAIRED UPDATE: keep the spike under __spike__/ or src/__tests__ so the production bundle-size gate is unaffected.

**Open questions:**
- Chip UX shape (3b-ii): the friendly/hostile chip has 3 outcomes but ConfirmDialog has 2 actions. Decide whether Phase 4 ships a minimal bespoke 2-choice+dismiss chooser reusing ConfirmDialog's portal/inert plumbing, or a slightly extended ConfirmDialog. Recommend bespoke to avoid overloading confirm/cancel semantics; Phase 6 hardens.
- Flag naming/registry: no generic feature-flag registry exists (only per-wrapper VITE_*_ENABLED reads). Confirm VITE_SCENE_DRAG_ENABLED as the single Phase-4 flag name and that it gates BOTH drag mount and PlacementMode hiding, so no divergent second flag drifts the mutual-exclusion invariant.
- Snap-back animation source: illegal drops must reuse runtime.ts's existing `issues` -> setActionIssues toast (around SceneManager L382). Confirm the DragLayer ghost has a snap-back transition or is simply dismissed on rejected emit (returns false).
- Gate budget number: the explicit post-drop reconcile frame-time budget and the continuous-repaint budget must be fixed (e.g. sub-16ms / sub-33ms) before task 11 can pass/fail. The plan leaves the number to the Phase-4 measurement; the reviewer/owner must ratify the threshold that funds Phases 5-6.
- data-coords vs WeakMap on SceneGridView: whether to add data-x/data-y attributes (simplest for elementFromPoint) or a cell->position WeakMap. data-attrs are the lowest-risk, a11y-neutral choice; confirm they don't perturb the memoized ~900-cell render cost.

## Phase 5 — Sheet eviction (click-to-add ONLY, no drop targets) + tab-grid collapse + micro-feedback + full RUNTIME_COPY_RULES guard discipline. Scope: 4 atomic per-system PRs (dnd5e[+dnd5e-2024, shared Dnd5eSheetBase], pf2e, d20-legacy, mam3e). dnd35e/pf1e/daggerheart have NO browser wrappers and are out of scope. INVARIANT preserved: no file under src/scene/ and none of the 12 SceneActionIntent variants are touched — Phase 5 is sheet-only, has zero scene surface, zero drag, zero intents; every content-add resolves to an EXISTING per-system controller handler.

**Anchors:** 12 verified, 2 drifted. **Definition of done:** "For each of the 4 in-scope systems (dnd5e[+2024], pf2e, d20-legacy, mam3e), one atomic PR has: (1) deleted the src/systems/** browser wrapper tabs/panels and removed their consumption + tab-strip triggers, recomputing the hardcoded grid-col literals; (2) wired dock click-to-add to the system's EXISTING controller add-handler via SheetDispatchContext, hard-gated on a resolved active-document id, with a toast + count-badge micro-feedback on the fast path; (3) KEPT every inline control (dnd5e onTogglePreparedSpell) and every reference-only tab (MamArchetypesTab); (4) preserved all 6 EDITED guarded tokens and, in PR A only, removed the Dnd5eFeatBrowserTab.tsx path from BOTH rules.ts and manifest.ts while keeping the browserSupport constant; (5) kept the touched named host <=400 LOC via sibling extraction; (6) rewritten the 4 affected test suites + shipped new sibling/micro-feedback tests; (7) run npm run verify green — including an explicit check:doc-drift, docDriftManifest parity, hostSizeBudget, knip, and coverage. The sheet has NO drop target, NO drag, NO chip, and stays always-editable (no canEdit gating added). src/scene/runtime.ts and the 12 SceneActionIntent variants are untouched (Phase 5 has no scene surface at all)."

**Drift found:**
- COUNT DRIFT: RUNTIME_COPY_RULES has 15 path-keyed entries, not the plan's 'THIRTEEN' (docs/doc-drift.rules.ts L291-391). All 6 EDITED + 1 DELETED Phase-5 anchors still hold at their exact cited lines; only the total count is stale. Update Finding-19 and Phase-7 prose to 'fifteen paths'.
- STRUCTURE DRIFT (understated in plan): the pf2e and d20-legacy spell 'browser panels' the plan lists as 'delete wrappers' are EMBEDDED inside the GUARDED Pf2eSpellsTab.tsx (L495) and D20SpellsTab.tsx (L443). Deleting them is therefore an EDIT to a guarded file, not a standalone wrapper delete — token preservation (and check:doc-drift) applies to those deletes too. The plan's file list separates them, obscuring this coupling.
- THIRD GUARD FILE (missing from plan): doc-drift is THREE coupled files, not two. src/__tests__/docDriftManifest.test.ts asserts RUNTIME_COPY_RULES paths == manifest.ts runtime-copy paths (set parity). Deleting the Dnd5eFeatBrowserTab.tsx entry from rules.ts WITHOUT the matching manifest.ts removal reddens this test (coverage/test gate), independent of check:doc-drift. The plan says 'rules.ts + manifest.ts'; it must say 'rules.ts + manifest.ts + docDriftManifest.test.ts parity'.
- CONSTANT-SURVIVAL TRAP (missing from plan): DND5E_FEAT_COPY.browserSupport is required by TWO rules — the Dnd5eFeatBrowserTab.tsx rule (removed in PR A) AND the src/utils/documentationCopy.ts rule (rules.ts L386, stays). The constant must NOT be deleted when the browser tab is deleted, or the documentationCopy.ts rule reddens. Plan implies the token simply 'drops'.
- TAB-GRID DRIFT: plan says '10-across tab strip'; actual Dnd5eTabsNavigation.tsx is xl:grid-cols-9 (10 only in dnd5e-2024 with weapon masteries), and the 'monsters' trigger is removed by PHASE 3 not Phase 5. Phase 5 removes only the conditional 'feats' trigger (L67-77) and must recompute the HARDCODED grid-col literals (L34-36).
- SCOPE CLARIFICATION: Phase 5 touches 4 systems only — dnd35e, pf1e, and daggerheart have NO browser wrapper components (verified) and daggerheart has no wrappers to evict; the plan's file list already reflects this but the 'per system, uniform across all 7' phrasing could mislead — the click-to-add verb is uniform, but only 4 systems have wrappers to delete.

**Tasks (ordered):**
0. PRECONDITION GATE (do not start Phase 5 until true): confirm Phase 3 landed src/context/SheetDispatchContext.tsx (generic onSelect published up from the active SheetSurface into a per-system handler registry) and the src/dock/ dock with its spell/feat/equipment tabs already click-adding via that context. Both are currently ABSENT (verified: no src/dock dir, no SheetDispatchContext.tsx). Phase 5 deletes the in-sheet browser wrappers because the DOCK now provides browse+add; without SheetDispatchContext there is nowhere to register the per-system add-handlers and eviction would strand add. If absent, STOP — Phase 3 is unmet. _(atomic w/ siblings)_
1. Build the shared micro-feedback + click-to-add sibling primitives ONCE (budget freed by the sheet having no drag). Create src/systems/shared (or src/components/ui) toast+count-badge-animation primitive and the per-host sibling hook useSheetClickToAdd.ts pattern: it (a) subscribes the host's EXISTING controller add-handlers into SheetDispatchContext so the dock's onSelect resolves to them, hard-gated on a resolved active-document id, and (b) fires the toast + count-badge animation on the fast (click-to-add) path. Host consumes it in 1-2 lines. Ship this primitive's unit tests in THIS PR (it is new tested code; do not let it ride an eviction PR untested). No src/systems/** value-import from shared — control stays inverted via SheetDispatchContext (respects the eslint layer boundary).
2. PR A — dnd5e (covers dnd5e + dnd5e-2024 via shared Dnd5eSheetBase). (1) Wire dock Feat click-add to controller.handleFeatSelect via useSheetClickToAdd; KEEP the inline Dnd5eSpellsTab onTogglePreparedSpell (L191) and the Equipment tab. (2) DELETE src/systems/dnd5e/shared/components/Dnd5eFeatBrowserTab.tsx and remove its consumption in Dnd5eSheetBase.tsx (L17 import, L249-254 JSX). (3) Collapse Dnd5eTabsNavigation.tsx: remove the conditional 'feats' TabsTrigger (L67-77) and RECOMPUTE the hardcoded grid-col literals L34-36 (currently xl:grid-cols-9 / xl:grid-cols-10) to the new tab count. (4) In the SAME commit: remove the Dnd5eFeatBrowserTab.tsx entry from rules.ts (L352-356) AND from manifest.ts (L315) — NEVER delete the DND5E_FEAT_COPY.browserSupport constant in documentationCopy.ts (still pinned by the documentationCopy.ts rule, rules.ts L386). (5) Measure Dnd5eSheetBase LOC (301 now; deletion of ~6 JSX lines minus 1-2 sibling-consumption lines nets it DOWN — must stay <=400). (6) Run check:doc-drift; ship useSheetClickToAdd registration + micro-feedback tests.
3. PR B — pf2e. (1) Wire dock Feat click-add to featBrowserProps.onSelectFeat and Equipment click-add to equipmentBrowserTabProps.onAdd; wire dock Spell click-add to the existing handleLearnSpell. (2) DELETE the two standalone wrappers Pf2eFeatBrowserTab.tsx (consumed pf2e/sheet.tsx L129) and Pf2eEquipmentBrowserTab.tsx (L142). (3) DELETE Pf2eSpellBrowserPanel.tsx — this is EMBEDDED in the GUARDED Pf2eSpellsTab.tsx (L495) so this is an EDIT to a guarded file: remove the <Pf2eSpellBrowserPanel> usage + import (L13) + preload (L501) while PRESERVING the two guarded tokens PF2E_SPELLS_COPY.alwaysPreparedSupport (L342) and .preparedSlotsSupport (L414). (4) Measure pf2e/sheet.tsx host LOC (155 now). (5) doc-drift: Pf2eSpellsTab.tsx path-entry STAYS in rules.ts/manifest.ts (file kept, tokens preserved) — no rules edit needed unless a token moves; run check:doc-drift to prove tokens still render. (6) Update the tab-nav for pf2e to drop the two removed browser tabs.
4. PR C — d20-legacy. (1) Wire dock Feat->onAddFeat, Equipment->onAddItem, Spell->onAddKnownSpell via SheetDispatchContext. (2) DELETE standalone D20FeatBrowserTab.tsx (D20LegacyTabs L355) and D20EquipmentBrowserTab.tsx (L393). (3) DELETE D20SpellBrowserPanel.tsx — EMBEDDED in the GUARDED D20SpellsTab.tsx (L443) so this is an EDIT to a guarded file: remove the panel usage + import (L11) while PRESERVING the D20_LEGACY_MANUAL_NOTES token (import L8, spread L101). (4) Collapse the tab strip in D20LegacyTabs.tsx (419 LOC — NOT a budgeted host, but keep the 31-LOC d20-legacy/sheet.tsx host well under 400). (5) doc-drift: D20SpellsTab.tsx path-entry STAYS (file kept, token preserved); run check:doc-drift.
5. PR D — mam3e. (1) Wire dock Power->powerBrowserTabProps add, Advantage->advantageBrowserTabProps add, Equipment->equipmentBrowserTabProps add via SheetDispatchContext. (2) DELETE MamPowerBrowserTab.tsx (mam3e/sheet L168), MamAdvantageBrowserTab.tsx (L172), MamEquipmentBrowserTab.tsx (L176). (3) KEEP MamArchetypesTab.tsx (reference-only, no add verb) with its guarded token MAM3E_ARCHETYPE_COPY.referenceOnly (L51) intact — no rules/manifest edit for it. (4) Collapse the mam3e tab strip; measure mam3e/sheet.tsx host LOC (185 now). (5) Run check:doc-drift (MamArchetypesTab token must still render).
6. After each per-system PR: re-run the doc-drift PARITY test src/__tests__/docDriftManifest.test.ts — it asserts RUNTIME_COPY_RULES.every(rule => manifest runtime-copy paths.has(rule.path)). This is the THIRD coupled guard file: any rules.ts path removed (only Dnd5eFeatBrowserTab in PR A) MUST be removed from manifest.ts in the same commit or this test reddens the coverage/test gate. No standalone task file — fold into each PR's checklist. _(atomic w/ siblings)_

**Test blast radius:**
- `src/__tests__/components/SpellBrowserPanels.test.tsx` — Imports Dnd5eSpellsTab (kept) AND Pf2eSpellBrowserPanel (L5, rendered L106 — DELETED in PR B). Remove the Pf2eSpellBrowserPanel import + its render assertions; MIGRATE the coverage to assert the browse-and-add now flows through the dock->SheetDispatchContext->handleLearnSpell path, OR move that assertion into a new dock-click-add test. If the file also references D20SpellBrowserPanel (deleted PR C), same treatment. Keep the Dnd5eSpellsTab assertions (that tab is kept).
- `src/__tests__/components/Dnd5eFeatManualBadge.test.tsx` — Matched the deleted-wrapper grep for Dnd5eFeatBrowserTab (deleted PR A). If it renders/imports Dnd5eFeatBrowserTab, repoint to the surviving featManualBadge util + the dock feat tab; if it only tests shouldShowDnd5eManualFeatBadge, leave the util assertions but drop the browser-tab render.
- `src/__tests__/components/Pf2eSpellsTab.test.tsx` — Pf2eSpellsTab is EDITED in PR B (embedded panel removed, tokens kept). Update any assertion that expected <Pf2eSpellBrowserPanel> inside the tab; ADD assertion that the two guarded tokens (alwaysPreparedSupport L342, preparedSlotsSupport L414) still render post-edit.
- `src/__tests__/d20-legacy-manual-extras.test.tsx` — D20SpellsTab is EDITED in PR C. Update assertions referencing the embedded D20SpellBrowserPanel; ADD assertion that D20_LEGACY_MANUAL_NOTES still renders (spread at L101) so the guarded token is proven present after the panel removal.
- `src/__tests__/docDriftManifest.test.ts` — No code change, but this test MUST pass after PR A removes the Dnd5eFeatBrowserTab.tsx path from BOTH rules.ts and manifest.ts (it asserts set-parity between RUNTIME_COPY_RULES paths and manifest runtime-copy paths). Run it as an explicit gate in PR A.
- `src/__tests__/hostSizeBudget.test.ts` — No edit expected, but it is a blast-radius GUARD: every PR that touches Dnd5eSheetBase/pf2e sheet/mam3e sheet/d20-legacy sheet must keep the host <=400 LOC. If sibling-extraction is done wrong and a host grows past 400 it reddens here. Add a per-PR checklist line measuring the touched host's LOC.
- `src/__tests__/(new) useSheetClickToAdd + micro-feedback specs` — NEW tests required in-PR (task 1 / each system PR): assert (a) dock onSelect resolves to the correct EXISTING controller handler (handleFeatSelect / handleLearnSpell / onAddItem / power-add etc.), (b) onSelect is hard-gated on a resolved active-document id (disabled, never targets a stale controller), (c) the toast + count-badge animation fires on the click-add fast path. Branch threshold (60) is the tightest gate — cover the gated/ungated branches.

**Acceptance gates:**
- npm run check:doc-drift — MUST run in EVERY per-system eviction PR (not just knip/hostSizeBudget/coverage). Proves the 6 EDITED guarded files still render their tokens (Dnd5eSpellsTab knownSpellCasting+alwaysPreparedSupport, Pf2eSpellsTab alwaysPreparedSupport+preparedSlotsSupport, D20SpellsTab D20_LEGACY_MANUAL_NOTES, Dnd5eSelectedFeatsSection selectedSupport, Dnd5eFeatureOptionsSection provenanceSupport+emptyState, MamArchetypesTab referenceOnly) and that the DND5E_FEAT_COPY.browserSupport constant survives in documentationCopy.ts after PR A.
- npm test -- docDriftManifest — asserts rules.ts↔manifest.ts runtime-copy path parity after PR A removes the Dnd5eFeatBrowserTab entry from both.
- npm test -- hostSizeBudget — all 5 named hosts <=400 LOC after every PR (Dnd5eSheetBase must not exceed 400; measure it printed value).
- npm run check:dead-code (knip) — no orphaned exports/deps after wrapper deletion; the deleted wrappers' now-unused imports must be cleaned.
- npm run test:coverage -- --run — lines70/functions65/branches60/statements70 hold; the new useSheetClickToAdd + micro-feedback siblings ship their own tests in the same PR that deletes the tested wrapper components (branch@60 is tightest).
- Playwright/vitest per system: from an open sheet, click-add a spell/feat/item from the dock and assert it appears on the sheet AND the toast + count-badge animation fires; assert the kept inline control still works (dnd5e onTogglePreparedSpell toggles prepared state); assert the deleted browser tab/trigger is GONE from the tab strip and the grid-col count shrank; assert the sheet exposes NO drop target / no drag affordance (Finding 8).
- npm run verify — the full CI gate green on each PR (includes lint layer-boundary: confirm no src/systems/** value-import of shared dock/context — control inverted via SheetDispatchContext).

**CI-gate impact:**
- doc-drift (rules.ts + manifest.ts + docDriftManifest.test.ts — THREE coupled files, corrected from the plan's two): PR A removes the Dnd5eFeatBrowserTab.tsx entry from rules.ts (L352-356) AND manifest.ts (L315) atomically; docDriftManifest.test.ts enforces the set-parity. PRs B/C EDIT guarded files (Pf2eSpellsTab, D20SpellsTab) but KEEP them — no rules/manifest path removal, only token-preservation proven by check:doc-drift. Never delete the DND5E_FEAT_COPY.browserSupport constant (pinned by the documentationCopy.ts rule, rules.ts L386).
- hostSizeBudget (per-file 400 LOC on 5 named hosts — sibling deletion does NOT reduce host LOC): paired update = measure Dnd5eSheetBase (301), pf2e/sheet (155), mam3e/sheet (185), d20-legacy/sheet (31) in each PR's checklist; land click-to-add as sibling modules consumed in 1-2 host lines.
- knip (dead-code): paired update = delete the wrappers' now-unused imports/exports; if a controller handler becomes reachable only via SheetDispatchContext, ensure it is not flagged unused (it is consumed by the registration sibling).
- coverage (lines70/functions65/branches60/statements70): paired update = each PR deletes tested wrapper components, so the same PR ships tests for the new useSheetClickToAdd registration + micro-feedback (branch@60 tightest); rewrite the 4 affected existing suites in-PR.
- eslint layer-boundary (shared must not value-import src/systems/**): paired update = the dock/SheetDispatchContext reach per-system controllers only via INVERTED control (systems register handlers up), never a shared-layer value-import of a system module.
- bundle-size: neutral-to-favorable (wrappers deleted); no paired update expected but keep an eye that the new toast/badge primitive is small.
- NOT touched: no scene/runtime.ts edit, no SceneActionIntent change, no chrome-dominance/interaction-budget scripts (those are Phase 6/7).

**Open questions:**
- Phase 3 dependency is currently UNMET in the repo: src/context/SheetDispatchContext.tsx and src/dock/ do not exist yet. Phase 5's entire click-to-add wiring assumes they exist. Confirm Phase 3 lands before Phase 5 starts (the spec's task 0 gates on this).
- The plan lists a per-host useSheetClickToAdd.ts 'new sibling' only under dnd5e, but pf2e/d20/mam3e all need the same registration+micro-feedback wiring. Decide whether useSheetClickToAdd is one shared hook parameterized per system (cleaner, but must avoid a shared-layer value-import of systems/**) or replicated per host. Recommend: a shared hook in a shared layer that TAKES the handlers as arguments (control still inverted), consumed by each host.
- dnd5e keeps its Spells AND Equipment tabs (only the Feat browser is evicted). Confirm the dock's Spell/Equipment tabs (Phase 3) are intended to COEXIST with dnd5e's kept in-sheet Spells/Equipment tabs, or whether dnd5e should also drop its in-sheet Equipment tab. The plan's dnd5e file list deletes only Dnd5eFeatBrowserTab, implying coexistence — the 'transient dual-home capped to Phase 5' risk note suggests dnd5e spell/equipment should ultimately be dock-only. Clarify the end-state for dnd5e spells/equipment.
- D20LegacyTabs.tsx is 419 LOC (already >400) but is NOT a budgeted host (the budgeted d20 host is the 31-LOC sheet.tsx). Confirm it is acceptable that the largest d20 tab container is unbudgeted, or whether Phase 5 should also thin it while collapsing.

## Phase 6 — Scene canvas (canvas-first): 5a decompose SceneSurface (emit seam + onLogToCampaign recap seam) / 5b right-rail summon tray + CollapsibleSection / 5c-i transform SceneCanvas render swap / 5d pan-zoom viewport (own gate) / 5e distance ruler + relocate allegiance chip + final PlacementMode deletion

**Anchors:** 16 verified, 3 drifted. **Definition of done:** ["SceneManager.tsx is decomposed into a thin SceneSurface that owns the zero-arg emit(intent) dispatch seam AND the App-owned onLogToCampaign(campaignId,title,body) recap callback; transient tool input state is pushed down into panels so the canvas no longer re-renders per keystroke; behavior is otherwise preserved.","The right 20rem operating rail is demoted into a summoned two-level tool tray (Dice/Check/Oracle/Reaction) + GM overlay (Initiative/Combat) built on a MOUNTED-not-unmounted CollapsibleSection with designed reopen affordances; DicePanel.history / in-flight drafts survive collapse (Playwright guard green).","SceneGridView.tsx is deleted and fully replaced by SceneCanvas.tsx — a fixed-1:1 transform/SVG-positioned render with a roving-tabindex a11y path, preserved role='grid'/'gridcell' aria-labels, and the Phase-4 grid drop target; knip is clean.","The pan/zoom SceneViewport subsystem (5d) lands as its own gated slice and PASSES its own gate: zoom-to-cursor lands the correct cell under variable scale+translate, and continuous pan/zoom repaint meets the frame budget on ~900 cells.","The distance ruler (5e) lands as a discrete overlay feature with grid-distance math and mutates no scene state.","The entire PlacementMode machine is deleted (all 18 verified reference sites, not just the named anchors), only after every kind's drag path (character/monster/marker) is live; generate-NPC is re-homed off the deleted `setPlacementMode('token')`; placement is drag-only with a drop-time classify chip.","The allegiance/kind chip is RELOCATED (not rebuilt) from Phase 4 into the canvas drop-time-classify flow, extended to kind where legal, resolving only to existing intents.","INVARIANT HELD: src/scene/runtime.ts and the 12 SceneActionIntent variants are untouched (git diff --exit-code); every gesture/drop emits an existing intent or controller handler; onLogToCampaign remains an App callback, not a 13th intent.","Acceptance tests green: recap-log appends to the linked campaign (Finding 4); no PlacementMode click-machine remains (Finding 21); right rail is summonable + chrome <=~10% (Finding 20); viewport gate passes (Finding 10).","npm run verify is green on the final Phase-6 PR set; each slice (5a / 5b / 5c-i / 5d / 5e) landed as its own gated sub-PR where landable, with the SceneCanvas swap + SceneGridView deletion atomic."]

**Drift found:**
- ZERO line-level drift: every Phase-6 anchor in the plan (SceneManager 1207 lines; PlacementMode type L65 / state L114 / badge L963 / toggles L1030/L1064/L1146 / cell handlers L528/L558/L574; LEFT rail L885; RIGHT rail L1007; roster documents.find L529; onLogToCampaign L79/L105; foldSceneEvents L155; buildPlacedToken tokenPlacement.ts:36-68) matches the CURRENT source EXACTLY. The plan was written against the present tree.
- COMPLETENESS GAP (not drift, but load-bearing): the plan's PlacementMode deletion anchor list is accurate but NON-EXHAUSTIVE. The full setPlacementMode/placementMode reference set is 18 sites; beyond the 8 named, the removal must also handle L145, L299, L550, L570, L591, L608 (deps), L642 (handleGenerateNpc arms 'token' — MUST be re-homed to the drag/auto-place flow, plan under-specifies), L685, L928, L966 (badge label), L1073 (adversary button label), and the daggerheart adversary placement block L1049-1078.
- CLARIFIED NON-IMPACTS: (a) doc-drift RUNTIME_COPY_RULES pins ZERO Phase-6 files — no rules.ts/manifest.ts edit needed for Phase 6 (unlike Phase 5). (b) hostSizeBudget budgets only the 5 src/systems/** sheet hosts — SceneSurface is not CI-LOC-gated, so 'thin' is a goal, not a gate.
- MOVE-TOKEN PATH PRESERVED: the click-to-move flow (selectedTokenId && click empty cell -> move-token, SceneManager L597-603) is NOT part of PlacementMode and survives the deletion; SceneCanvas must keep it (SceneManager.test.tsx L240-243 asserts it).
- DEPENDENCY REALITY: SceneSurface, SceneDispatchContext/emit seam, surfaces/ dir, the Phase-4 ConfirmDialog chip, and the drag engine do NOT exist yet (repo at Phase 0). Phase 6 is blocked until Phases 1/2/4 deliver them; the spec assumes they land per plan.

**Tasks (ordered):**
1. 5a DECOMPOSE: create/flesh out src/components/surfaces/SceneSurface.tsx (Phase-2 stub) as the thin host that owns (a) the zero-arg emit(intent:SceneActionIntent)=>boolean dispatch published via SceneDispatchContext (Phase 4), bound internally to the shell-resolved selectedScene, and (b) the App-owned onLogToCampaign(campaignId,title,body) recap callback. Move SceneManager's orchestration (foldSceneEvents/state derivation L148-169, emitSceneAction L356-391, combat/initiative/check/oracle handlers) into SceneSurface or co-located hooks; the 12 SceneActionIntents and runtime.ts stay untouched — every handler emits an existing intent via emit. Do NOT fold onLogToCampaign into an intent (it is a 13th-seam App callback). _(atomic w/ siblings)_
2. 5a PUSH TRANSIENT INPUT STATE DOWN: relocate each tool's transient input useState (tokenName/tokenKind/tokenAllegiance/tokenStatblockId/tokenDocumentId L116-123, markerLabel/kind/width/height L124-127, initiativeValues L131, combatTargetId L133, encounter draft inputs) INTO the owning panel components so a keystroke no longer re-renders the whole scene subtree/canvas. Behavior-preserving: the emit calls still fire the same intents with the same payloads. _(atomic w/ siblings)_
3. 5a THREAD THE RECAP SEAM: wire onLogToCampaign App.tsx handleLogSceneToCampaign (L132-137) -> SurfaceStage -> SceneSurface prop -> RecapPanel.onLog, preserving the (campaignId,title,body) signature and the `onLogToCampaign && linkedCampaign` render gate (SceneManager L1189). Ship the recap-log acceptance test (Finding 4) in THIS PR: from an active scene with a linked campaign, log a recap, assert the campaign's session log gains the entry (createSessionEntry/addSessionEntry path). _(atomic w/ siblings)_
4. 5b CREATE CollapsibleSection.tsx: a MOUNTED + CSS-hidden (visibility/hidden attribute, NEVER unmount) collapsible with a designed reopen affordance (header button, not a bare boolean). Ship a Playwright persistence guard: put an in-flight DicePanel roll history into a collapsed section, reopen, assert history survives (Finding: unmount destroys DicePanel.history / in-flight drafts).
5. 5b DEMOTE THE RIGHT OPERATING RAIL (its FIRST demotion, Finding 20): replace the docked 20rem right column (SceneManager L1007 grid) with a summoned two-level tool rail built on CollapsibleSection — fold Dice/Check/Oracle/Reaction into a summonable tray, fold Initiative+Combat into a GM overlay, and fold the token/monster SOURCE into the dock. Every panel reopens via a designed affordance. This is where 'canvas dominates' becomes true; it unblocks the Phase-7 chrome-dominance script. Acceptance: assert the right rail is summonable and non-content chrome <=~10% of Scene viewport pixels.
6. 5c-i CREATE SceneCanvas.tsx: a from-scratch transform/SVG-positioned canvas at FIXED 1:1 scale that REPLACES SceneGridView.tsx. Re-implement a roving-tabindex keyboard-grid a11y path (SceneGridView uses tabIndex=0 on every cell — SceneCanvas moves to one tab-stop + arrow-key roving). PRESERVE the existing test surface: role='grid'/'gridcell', aria-label `Cell x,y[, marker][, tokens]`, onCellActivate(SceneCoordinate) and onTokenActivate(token) contracts, and the move-token click path (select token -> click cell -> move-token intent). Attach the grid drop target from the Phase-4 spike (clientX/Y -> integer SceneCoordinate at fixed scale); every drop emits an EXISTING intent (place-token/move-token/add-marker) via the zero-arg emit seam. This slice adds NO pan/zoom. Budget the post-drop reconcile of ~900 cells (pre-measured by the Phase-4 gate). _(atomic w/ siblings)_
7. 5c-i RETIRE SceneGridView.tsx: delete src/components/SceneGridView.tsx (remove the L51 import in SceneManager/SceneSurface) and delete or repoint src/__tests__/components/SceneGridView.test.tsx to SceneCanvas — MUST be atomic with task 6 or knip flags a dead export and coverage drops. Preserve/move the pure helpers still used (cellKey/footprintCells from scene/grid, tokenAllegiance from scene/allegiance, TokenHpBar) into SceneCanvas. _(atomic w/ siblings)_
8. 5e RELOCATE/HARDEN the allegiance/kind chip: move the Phase-4 (3b-ii) ConfirmDialog-based friendly/hostile inline chip into SceneCanvas's drop-time-classify flow and extend it to kind (character/adversary/marker) where legal. Resolves to EXISTING intents only (place-token, set-token-allegiance, add-marker); scene-generic, not per-system (respects the eslint layer boundary — no value-import of src/systems/**). Illegal drops surface runtime.ts's existing `issues` as a toast + snap-back.
9. 5e WIRE THE MARKER DRAG PATH (last uncovered kind, Finding 21): give markers a drag-to-place path (drag from the marker source/dock -> drop -> add-marker intent) so the marker PlacementMode entry (L1146 toggle, L574 cell handler) can be removed. Until this lands, markers keep PlacementMode; this task is the precondition for the final machine deletion. _(atomic w/ siblings)_
10. FINAL PLACEMENTMODE DELETION (Finding 21) — only after tasks 8-9 confirm every kind's drag path (character 3b-i, monster 3b-ii, marker task 9) is live. Remove the ENTIRE machine — NOT just the plan's named anchors. Full 18-site sweep verified in current source: type L65, state L114, reset in scenes-effect L145, onEncounterApplied reset L299, cell-handler branches L528/L558/L574 + their post-place resets L550/L570/L591, deps-array entry L608, handleGenerateNpc arming L642 (RE-HOME: generate-NPC must now seed the drag/auto-place flow, not `setPlacementMode('token')` — plan under-specifies this), handleTokenActivate reset L685, rail-click reset L928 (rail already relocated in Phase 1), badge L963-968 incl. label L966, token toggle L1030-1033, adversary block toggle+label L1064/L1073, marker toggle L1146-1149. Replace wholesale with drop-time classify on the canvas. _(atomic w/ siblings)_
11. 5d PAN/ZOOM VIEWPORT subsystem — OWN GATED SLICE (Finding 10), 100% greenfield. Create SceneViewport.ts(x): a viewport state machine (scale + translate), wheel-zoom-to-cursor with clamping (floor((clientX-rect.left-tx)/(cellPx*scale))), drag-pan, and PWA touch pinch-zoom + one-finger-pan arbitration. Integrate into SceneCanvas so coordinate inversion works under variable scale+translate. Carries its OWN acceptance gate (do NOT describe the math as 'already retired in Phase 4' — Phase 4 only PROBED it). Depends on task 6 (SceneCanvas).
12. 5e DISTANCE RULER slice (Finding 10): create SceneDistanceRuler.tsx — a discrete measurement feature (new drag gesture + overlay + grid-distance math via scene/grid), landed separately from render (task 6) and viewport (task 11). No new intent; overlay-only, does not mutate scene state.
13. TEST BLAST RADIUS: rewrite SceneManager.test.tsx placement/move assertions (L223-289) from click-'Place token/marker'->click-gridcell to the drag-drop path (drag harness or direct drop-handler invoking place-token/add-marker via emit); keep move-token select->click-cell (L240-243) working against SceneCanvas; verify capabilityScenarios.test.tsx S1/S14 (engine-level, should be unaffected) and sceneCombatBridgeUi.test.ts still green after decompose. Add acceptance tests: recap-log (task 3), 5d viewport-gate, 5b right-rail-summon + chrome-dominance, no-placement-machine (assert no 'Place token'/'Place Adversary'/'Place Marker' toggle exists; placement is drag-only). _(atomic w/ siblings)_

**Test blast radius:**
- `src/__tests__/components/SceneManager.test.tsx` — REWRITE the three placement tests (L223 'places a linked token...', L257 'places a mechanically-real NPC...', L277 'places and removes...markers'). They currently drive click(button /place token|place marker/) then click(gridcell) — those toggle buttons are DELETED with PlacementMode. Replace with the drag-drop path (drag-simulation harness or direct SceneCanvas drop-handler that emits place-token/add-marker via the emit seam). KEEP the move-token assertion (L240-243: select Token Astra -> click Cell 4,4 -> assert 'Cell 4, 4, Astra') since the click-move path survives. Grid role='gridcell' + aria-label `Cell x, y[, name]` assertions must still resolve against SceneCanvas.
- `src/__tests__/components/SceneGridView.test.tsx` — DELETE (126 lines) or rename to SceneCanvas.test.tsx and repoint. It asserts onCellActivate({x:3,y:2}) on click (L108-112), onTokenActivate on token click (L113), keyboard Enter->onCellActivate (L120-124), and gridcell aria-labels (L88). SceneCanvas must satisfy the equivalent contract, so port these assertions rather than dropping coverage (branch threshold 60 is tightest).
- `src/__tests__/scenarios/capabilityScenarios.test.tsx` — VERIFY only — S1 (L130, runtime+placement+initiative+combat) and S14 (L515, re-side after placement) operate at the runtime/summarizeSceneForLog level, not the PlacementMode UI, so they should be unaffected by the UI decomposition. Confirm any scene-selection cases still pass with the Phase-1 controlled selectedSceneId prop.
- `src/__tests__/rules/sceneCombatBridgeUi.test.ts` — VERIFY green after 5a decompose — named in the plan's Phase-6 blast radius. Confirm it does not assert on the deleted PlacementMode toggles or the retired SceneGridView; update mounts if it renders SceneManager directly (now SceneSurface).
- `src/__tests__/components/SceneCanvas.test.tsx` — NEW — render at fixed 1:1, drop emits the correct EXISTING intent via a mocked emit seam (place-token for a party character-doc, add-marker for a marker), roving-tabindex keyboard navigation (one tab-stop + arrow roving, Enter/Space activates onCellActivate), and the preserved role='grid'/'gridcell' aria-labels.
- `src/__tests__/components/SceneViewport.test.ts` — NEW (5d own gate) — coordinate inversion under variable scale+translate (assert clientX/Y -> correct integer cell after zoom-to-cursor), clamping at min/max scale and pan bounds, and a continuous-repaint frame budget assertion on ~900 transform-positioned cells (performance.measure).
- `src/__tests__/components/SceneSurface.recap.test.tsx` — NEW (Finding 4) — from an active scene with a linked campaign, invoke RecapPanel.onLog and assert onLogToCampaign(campaignId,title,body) fires and the campaign session log gains the entry; guards against the recap write being silently dropped during decomposition.
- `e2e/scene-canvas.spec.ts` — NEW Playwright acceptance — (a) 5d viewport gate: zoom-to-cursor lands the correct cell under variable scale + continuous pan/zoom meets frame budget; (b) 5b: right operating rail is summonable via a designed affordance AND chrome <=~10% of Scene viewport; (c) no-placement-machine: assert no 'Place token'/'Place Adversary'/'Place Marker' toggle button exists — placement is drag-only; (d) CollapsibleSection persistence: collapse a tray with in-flight dice history, reopen, history survives.

**Acceptance gates:**
- npm run test -- src/__tests__/components/SceneManager.test.tsx src/__tests__/components/SceneCanvas.test.tsx src/__tests__/components/SceneViewport.test.ts (placement now drag-driven, move-token preserved, canvas render + coordinate inversion green)
- npm run test -- src/__tests__/components/SceneSurface.recap.test.tsx (recap-log seam: logging a recap appends to the linked campaign's session log — Finding 4)
- npm run test:e2e -- e2e/scene-canvas.spec.ts (no-placement-machine: placement is drag-only, no Place* toggle; 5d zoom-to-cursor lands the correct cell under variable scale + repaint frame budget on ~900 cells; 5b right rail summonable + chrome <=~10%; CollapsibleSection persistence)
- npm run check:dead-code (knip clean — SceneGridView.tsx fully removed, no dead export; SceneCanvas/SceneViewport/SceneDistanceRuler/CollapsibleSection all reachable from an entry)
- npm run typecheck (SceneDispatchContext emit(intent):boolean bound to selectedScene; onLogToCampaign(campaignId,title,body) threaded; SceneActionIntent union unchanged)
- npm run test:coverage (lines70/functions65/branches60/statements70 — new branch-heavy SceneCanvas/SceneViewport ship unit tests OR DOM-only pieces added to vitest.config coverage exclude in the same PR)
- npm run lint (eslint layer-boundary: SceneSurface/SceneCanvas in src/components/** do NOT value-import src/systems/**; classify chip is scene-generic)
- git diff --exit-code src/scene/runtime.ts src/types/core/scene.ts (INVARIANT: runtime.ts + the 12 SceneActionIntent variants untouched)
- npm run verify (full CI gate green on the final Phase-6 PR set)

**CI-gate impact:**
- knip (dead-code): retiring SceneGridView.tsx MUST delete the file + its test atomically with the SceneCanvas swap, or knip flags the orphaned export/module. New files (SceneCanvas/SceneViewport/SceneDistanceRuler/CollapsibleSection) must each be imported from a reachable entry. NO paired rules.ts/manifest.ts edit — this is a code-only gate.
- coverage (lines70/fn65/branches60/statements70): deleting SceneGridView.test.tsx removes covered lines; branch-heavy SceneCanvas + SceneViewport + classify-chip must ship unit tests in-PR, OR add DOM-only/Playwright-only pieces (DragLayer overlay, viewport pointer handlers) to vitest.config.ts coverage `exclude` in the SAME PR. Paired update: vitest.config.ts.
- doc-drift (RUNTIME_COPY_RULES, docs/doc-drift.rules.ts + manifest.ts): NON-IMPACT — verified NONE of Phase 6's files (SceneManager/SceneGridView/SceneCanvas/RecapPanel/ConfirmDialog/scene panels) is among the 13 file-path-keyed pins (all docs + the six Phase-5 system inline files). No rules.ts/manifest.ts edit required for Phase 6. (Distinct from Phase 5, which edits guarded inline files.)
- hostSizeBudget (src/__tests__/hostSizeBudget.test.ts, <=400 LOC): NON-IMPACT — the 5 budgeted hosts are all src/systems/** sheet hosts; no scene file is budgeted. 5a's 'thin SceneSurface' is a design goal, not CI-enforced. Optional paired update if the team wants it guarded: add src/components/surfaces/SceneSurface.tsx to HOSTS (out of Phase-6 scope).
- bundle-size: SceneCanvas + SceneViewport greenfield adds LOC, offset by the SceneManager decompose + SceneGridView deletion. Keep the whole scene subtree behind App.tsx React.lazy (L30-31) so it stays out of the eager index chunk.
- chrome-dominance (scripts/check-chrome-dominance.mjs, added in Phase 7 but ANCHORED to Phase 6): 5b's right-rail demotion is the precondition that makes non-content chrome <=~10% achievable; assert it from the post-5b state, never Phase 1.
- eslint layer-boundary: SceneSurface/SceneCanvas (shared src/components/**) must not value-import src/systems/**; the emit(intent) seam via SceneDispatchContext and the scene-generic classify chip keep control inverted. INVARIANT paired check: runtime.ts + the 12 intents are never edited (git diff --exit-code guard).

**Open questions:**
- Generate-NPC re-home (SceneManager L627-643, handleGenerateNpc): today it seeds token inputs and arms placementMode='token'. With PlacementMode deleted, what is the new gesture? Options: (a) auto-place the generated NPC at a default cell via a place-token emit, or (b) seed a drag payload the GM then drags. Plan does not specify — decide before the final PlacementMode deletion (task 10).
- Move-token verb on the canvas: keep the existing click-select-then-click-cell move (preserved), OR upgrade to drag-to-move? Plan focuses drag on PLACEMENT and is silent on move; recommend preserving the click path and optionally adding drag-move as polish, but confirm.
- 5d frame budget number: the plan references a 'post-drop reconcile frame-time budget' pre-measured by the Phase-4 gate and a 'continuous-repaint budget' — the concrete ms/FPS threshold for the 5d gate must be inherited from the Phase-4 spike output, which does not exist yet. The 5d acceptance test needs that number.
- Marker drag source (task 9): where does the marker drag originate — the demoted MarkerPanel in the summon tray, or a dock tab? The party+monster sources are dock-based (Phase 3/4); markers have no dock tab, so the drag source location needs a decision to complete the 'all kinds covered' precondition for the final PlacementMode deletion.
- Should SceneSurface be added to hostSizeBudget HOSTS to CI-enforce the 5a 'thin' decomposition? Currently unguarded; team preference.

## Phase 7 — Hardening: hash-sync restore-on-reload + chrome-dominance & interaction-latency CI gates (anchored post-Phase-6) + formalize the coverage/host-LOC/doc-drift constraint set of record + app-facing seam documentation + owner usability gate. INVARIANT preserved: src/scene/runtime.ts and the 12 SceneActionIntent variants are never edited; Phase 7 adds no gesture and touches no intent — it only serializes nav, adds two measurement gates, and writes docs.

**Anchors:** 11 verified, 4 drifted. **Definition of done:** "Reload restores the exact {surface, librarySegment, sheetDocId, sheetDocId→sheet, sceneId, overlay} nav position via history.replaceState hash-sync in useAppNav.ts with no react-router, and stale/invalid ids are sanitized (missing doc → closeSheet/library; missing scene → existing auto-reset) so reload can never strand or crash; the ShellNavState assertNever totality still compiles. scripts/check-interaction-budget.mjs and scripts/check-chrome-dominance.mjs exist, are wired into `npm run verify` after test:e2e, consume Playwright-emitted artifacts, and are green on the post-Phase-6 build — with chrome-dominance provably anchored to the post-5b summoned-tray Scene (Finding 20), not Phase 1. The constraint-set-of-record doc records: coverage 70/65/60/70 (+ App/main exclusion rationale), hostSizeBudget 5 named hosts @400 LOC, and the doc-drift RUNTIME_COPY_RULES guard as a FILE-PATH-keyed token guard over FIFTEEN entries at L291-391 (correcting the plan's stale '13 paths L295-381', six of them Phase-5-EDITED), plus a per-phase checklist item covering coverage-delta, host LOC, and the edit-OR-delete guarded-path check. The seam doc enumerates all five app-facing seams (five-writer selectedSceneId+onSelectScene, zero-arg emit(intent), App-owned onLogToCampaign, six-writer openSheet + re-homed Export/Delete, party-tab drag source) with control-inversion noted for SheetDispatchContext/SceneDispatchContext, documents the role toggle as disclosure-only and read-only/view-mode as out of scope, and every cited anchor resolves in source. The seam context files sit under a coverage-included path. src/scene/runtime.ts and the 12 SceneActionIntent variants are untouched; no 13th intent added. `npm run check:doc-drift` and full `npm run verify` pass. The owner's per-chapter usability sign-off is recorded as the explicit gate for the next interaction chapter (5d pan/zoom), and `graphify update .` + `/save` have refreshed the graph and memory/hot.md."

**Drift found:**
- MAJOR — the plan's headline Phase-7 fact is stale: doc-drift RUNTIME_COPY_RULES is NOT 'thirteen paths at L295-381'. Current truth (verified): a FIFTEEN-entry array at docs/doc-drift.rules.ts L291-391. The two the plan's '13' most plausibly omitted are the runtime .mjs pins (scripts/runtime/runtime-policy.mjs L293, scripts/check-node-version.mjs L302); docDrift.ts iterates all 15. Phase 7's constraint-of-record doc MUST say 15 (or derive from RUNTIME_COPY_RULES.length) — writing '13' verbatim would make the very doc that formalizes the guard a new drift/inaccuracy.
- MINOR — Dnd5eFeatBrowserTab browserSupport pin is at L353 (requiredToken L354), plan cited 'L355'. Off by one, immaterial; the six Phase-5-EDITED inline pins (Dnd5eSpellsTab L340, Pf2eSpellsTab L366, D20SpellsTab L335, Dnd5eSelectedFeatsSection L348, Dnd5eFeatureOptionsSection L358, MamArchetypesTab L374) all match exactly.
- PATH INCONSISTENCY — the plan's Phase 2/3/4 file lists write the seam contexts under src/context/ (singular: ShellContext/SheetDispatchContext/SceneDispatchContext), but the repo's established directory is src/contexts/ (plural: AuthContext.tsx, auth-context.ts) and vitest coverage.include globs 'src/contexts/**' (plural). Singular-path files would escape the coverage gate. Phase 7 (which formalizes the coverage constraint) must reconcile — recommend the plural path, else widen the include glob in-commit.
- CONFIRMED-ABSENT — no role/GM/player/read-only/view-mode construct exists in current src/components or src/hooks. The role toggle is genuinely net-new; Phase 7's disclosure-only doc codifies the deliberate absence of any enforcement path (consistent with Decision B), not a change to an existing toggle.
- NO DRIFT (positively re-grounded) — all six sheet-open writer anchors (App.tsx L276/L328/L346/L435/L585/L618) + three closeSheet sites (L190/L286/L580), all five scene-selection writers (SceneManager L109/L143/L807/L872/L926), the onLogToCampaign recap seam (App L631, SceneManager L79/L105), the 18rem left rail (L885) and 20rem right rail (L1007), the vitest thresholds (70/65/60/70), and the five hostSizeBudget hosts @400 LOC all HOLD exactly as the plan cites. useAppNav.ts and the seam contexts are not-yet-present Phase-1/2 deliverables (expected).
- VERIFY-CHAIN — `npm run verify` currently ends at ...test:e2e && check:repo-hygiene && ... check:compute-register; it contains no interaction/chrome gate. App.tsx and main.tsx are intentionally coverage-EXCLUDED, so the Phase-1 performance instrument in App.tsx is not unit-covered — the interaction-latency gate must be Playwright-driven (artifact + .mjs), which the spec adopts.

**Tasks (ordered):**
1. Create a PURE, unit-testable hash codec module under a COVERAGE-INCLUDED path: src/hooks/shellNavHash.ts. Export encodeShellNav(state: ShellNavState): string and decodeShellNav(hash: string): Partial<ShellNavState>. Serialize all five fields {surface, librarySegment, sheetDocId, sceneId, overlay} into a compact hash (e.g. #surface=scene&seg=characters&doc=<id>&scene=<id>&overlay=<id>). decode must tolerate malformed/empty/partial hashes and unknown enum values by dropping them (never throw). Keep the codec framework-agnostic so it earns unit coverage without a DOM (App.tsx/main.tsx are coverage-excluded; src/hooks/** is included).
2. Wire hash-sync into useAppNav.ts / the Phase-2 ShellContext reducer: (a) on first mount, seed initial ShellNavState from decodeShellNav(location.hash), falling back to the Phase-1 default (Characters/library) for any absent field; (b) on every nav change, write encodeShellNav(state) via history.replaceState (NOT pushState — no history pollution, matches 'deferred router / no deep-linking' decision), debounced to coalesce rapid switches. Preserve the assertNever totality over BOTH discriminants (typecheck:test must still compile). _(atomic w/ siblings)_
3. Sanitize stale/invalid ids on restore so a reload can never strand or crash: if the restored sheetDocId is not in documents, route through the existing closeSheet/back path (App L190/L286/L580 semantics) and land on library; if the restored sceneId is not in scenes, let the existing SceneManager L142-143 auto-reset (scenes[0]?.id ?? null) resolve it; if an overlay id no longer resolves, drop it. No new intent, no runtime.ts edit. _(atomic w/ siblings)_
4. Promote the Phase-1 performance.mark/measure surface-switch instrument into a CI gate. Add a Playwright spec that drives a surface switch, reads the performance measures via page.evaluate(() => performance.getEntriesByType('measure')), and writes a metrics artifact to a fixed path (e.g. test-results/perf/interaction-latency.json). Then create scripts/check-interaction-budget.mjs that reads that artifact and asserts the switch latency <= the Phase-1-measured frame budget, process.exit(1) on miss or missing artifact.
5. Create the chrome-dominance gate ANCHORED TO POST-PHASE-6 STATE (Finding 20). Add a Playwright spec that navigates to the Scene surface (post-5b summoned-tray layout), computes non-content chrome pixel area (the ~52px header rail + any docked/summoned chrome bounding boxes) divided by viewport pixel area via getBoundingClientRect, and writes test-results/perf/chrome-dominance.json. Create scripts/check-chrome-dominance.mjs asserting ratio <= ~0.10 (non-content chrome <= ~10% of Scene viewport), exit(1) on miss. Document that this gate is only satisfiable after Phase-6 5b demotes the 20rem right rail (SceneManager L1007) — it would fail against the Phase-1 docked rail (~320px).
6. Wire both new gates into package.json: add scripts check:interaction-budget => node scripts/check-interaction-budget.mjs and check:chrome-dominance => node scripts/check-chrome-dominance.mjs, and insert them into the verify chain IMMEDIATELY AFTER test:e2e (they consume Playwright artifacts): ...&& npm run test:e2e && npm run check:interaction-budget && npm run check:chrome-dominance && npm run check:repo-hygiene && ... _(atomic w/ siblings)_
7. Reconcile the seam-context coverage path (Finding from anchor drift): decide the of-record location. RECOMMENDED — move/create the Phase-2/3/4 seam contexts under src/contexts/ (plural, already coverage-included and the repo convention) rather than src/context/ (singular). If they must stay at src/context/, add 'src/context/**/*.{ts,tsx}' to vitest.config.ts coverage.include in the SAME commit. Record the decision in the constraint doc so no seam file silently escapes the coverage gate.
8. Write the CONSTRAINT-SET-OF-RECORD doc formalizing every CI gate the four red-team passes surfaced, with the CORRECTED numbers: (a) vitest coverage thresholds lines 70/functions 65/branches 60/statements 70 (branches 60 is tightest) and the App.tsx/main.tsx coverage exclusion rationale; (b) hostSizeBudget = per-file <=400 LOC on the FIVE named hosts (list them); (c) doc-drift RUNTIME_COPY_RULES = a FILE-PATH-keyed token guard over FIFTEEN entries at docs/doc-drift.rules.ts L291-391 (NOT the plan's '13 paths L295-381' — correct this and note six of the fifteen are Phase-5-EDITED inline files); (d) knip dead-code, hostSizeBudget, bundle-size. Add a per-phase checklist item: 'measure coverage-delta, measure host LOC for the 5 hosts, and confirm every deleted OR edited file whose path appears in RUNTIME_COPY_RULES has its tokens preserved or its rules.ts+manifest.ts paired.'
9. Write the SEAM-DOCUMENTATION section (same doc) enumerating all FIVE app-facing seams with their as-built current anchors: (1) the compound FIVE-writer scene-selection seam selectedSceneId+onSelectScene (SceneManager L109/L143/L807/L872/L926; L807/L872 relocated to LibraryScenesView in Phase 1); (2) the zero-arg emit(intent: SceneActionIntent)=>boolean bound to selectedScene, consumer DragLayer (Phase 4); (3) the App-owned onLogToCampaign(campaignId,title,body) recap callback (App L631, SceneManager L79/L105 — NOT one of the 12 intents, owned by SceneSurface post-Phase-6); (4) the compound SIX-writer openSheet(docId) seam (App L276/L328/L346/L435/L585/L618) plus re-homed Export/Delete + closeSheet sites (L190/L286/L580); (5) the party dock-tab drag source resolved against documents (Phase 4). State the invariant: SceneDispatchContext + SheetDispatchContext INVERT control so no shared/shell layer value-imports src/systems/** (eslint boundary). Document the role toggle as disclosure-only and read-only/view-mode as explicitly OUT OF SCOPE (no onUpdate=canEdit enforcement path).
10. Pair the new docs with doc-drift and run check:doc-drift. If any figure in the constraint doc restates machine-checked truth (the 15-path count, the 5 host names, the coverage thresholds), either derive it programmatically or add the doc to docs/doc-drift.manifest.ts pairing so future edits stay honest. CRITICAL: do NOT hardcode the plan's stale '13' — the doc must state 15 (or read the count from RUNTIME_COPY_RULES.length) so the constraint-of-record does not itself become drift. Phase 7 edits NO RUNTIME_COPY_RULES-guarded source file, so docs/doc-drift.rules.ts token pins are unchanged. _(atomic w/ siblings)_
11. Run the owner usability gate + memory hygiene: record the owner's per-chapter 'feels buttery' sign-off as the explicit gate for whether the next interaction chapter (specifically the 5d pan/zoom subsystem) is warranted; then run `graphify update .` and `/save` (update memory/hot.md) so the constraint set, seam catalogue, and gate decision are captured for the next session.

**Test blast radius:**
- `src/__tests__/hooks/shellNavHash.test.ts (new)` — Unit-test the pure codec: (a) round-trip — encode then decode reproduces all five fields {surface, librarySegment, sheetDocId, sceneId, overlay}; (b) empty hash ('' / '#') decodes to {} (caller applies defaults); (c) malformed hash never throws and drops unparseable pairs; (d) unknown enum values (surface='bogus', segment='bogus') are dropped, not passed through — so the reducer's assertNever contract is never violated by attacker-controlled hash; (e) partial hash (only doc set) decodes just that field.
- `e2e/restore-on-reload.spec.ts (new)` — Playwright: set a non-default nav state (open Scene surface with a selected scene AND open a sheet doc AND a library segment AND an overlay), reload the page, assert the exact surface + librarySegment + sheetDocId + sceneId + overlay are restored with no flash/crash. Second case: inject a hash with a stale sheetDocId that is not in documents, reload, assert the app lands on library via the closeSheet path (no crash). Third case: stale sceneId → assert auto-reset to scenes[0].
- `e2e/interaction-budget.spec.ts (new)` — Drive a surface switch, read performance measures via page.evaluate, and write test-results/perf/interaction-latency.json. Assert inline that the measure exists and is under budget (so a regression fails the e2e run too), AND emit the artifact that scripts/check-interaction-budget.mjs re-asserts in the verify chain.
- `e2e/chrome-dominance.spec.ts (new)` — On the post-Phase-6 Scene surface, compute non-content-chrome-pixels / viewport-pixels via getBoundingClientRect and write test-results/perf/chrome-dominance.json. Assert ratio <= ~0.10. Include a guard note that this runs against the summoned-tray layout, not the docked 20rem rail.
- `scripts/check-interaction-budget.mjs + scripts/check-chrome-dominance.mjs self-check` — Add a lightweight fixture assertion (or a vitest spec under src/__tests__ importing the pure threshold function extracted from each .mjs) proving exit-1 on an over-budget artifact and exit-0 under budget, and exit-1 on a missing artifact — otherwise the gate could silently pass when Playwright fails to emit.
- `src/__tests__/App.test.tsx / src/__tests__/scenarios/capabilityScenarios.test.tsx` — If hash-init changes boot behavior, add/adjust an assertion that a clean boot (empty hash) still lands on the Characters/library default and that a seeded hash initializes the corresponding surface. These render(<App/>) suites are in the Phase-1 blast radius and must stay green.
- `src/__tests__/hostSizeBudget.test.ts` — No assertion change (useAppNav.ts is not a host). Re-affirmed as the of-record 5-host/400-LOC gate; confirm the hash-sync additions did not push any of the 5 named hosts over 400 LOC (they touch hooks/, not hosts).

**Acceptance gates:**
- npm run verify passes end-to-end WITH the two new gates (check:interaction-budget, check:chrome-dominance) in the chain after test:e2e.
- npx playwright test e2e/restore-on-reload.spec.ts — reload restores exact {surface, librarySegment, sheetDocId, sceneId, overlay}; stale sheetDocId lands on library (closeSheet path) with no crash; stale sceneId auto-resets to scenes[0].
- npx vitest run src/__tests__/hooks/shellNavHash.test.ts — codec round-trips all five fields and drops malformed/unknown values without throwing.
- npm run typecheck:test — the ShellNavState assertNever over both discriminants still compiles after hash-init wiring (totality preserved).
- node scripts/check-interaction-budget.mjs exits 0 on an under-budget artifact and 1 on an over-budget/missing artifact (fixture-proven).
- node scripts/check-chrome-dominance.mjs asserts non-content chrome <= ~10% of Scene viewport on the post-Phase-6 build, exit 1 on miss; demonstrably would fail against the Phase-1 docked-20rem-rail layout (anchor-to-Phase-6 proof, Finding 20).
- npm run check:doc-drift stays green — no RUNTIME_COPY_RULES-guarded source file is edited by Phase 7, and the new constraint doc states the CORRECT count (15) so it is not a fresh drift source.
- grep proof that src/scene/runtime.ts is unchanged in the Phase-7 diff and no 13th SceneActionIntent was added (frozen-core invariant).
- The seam+constraints doc enumerates all five app-facing seams + role-disclosure-only + read-only-out-of-scope, and every seam anchor it cites resolves in current source.

**CI-gate impact:**
- package.json verify chain: +check:interaction-budget and +check:chrome-dominance inserted after test:e2e (they consume Playwright-emitted JSON artifacts). Paired update: package.json scripts block + the two scripts/*.mjs + the two e2e specs that emit the artifacts, all in the same PR.
- coverage (lines70/functions65/branches60/statements70): the pure codec src/hooks/shellNavHash.ts is under a coverage-INCLUDED path and MUST ship its own unit test (branches 60 is tightest). The two .mjs gate scripts and the three e2e specs are Playwright/Node-only, not unit-instrumented — either extract each .mjs's threshold predicate into a covered, unit-tested helper OR (if a DOM/script module is genuinely un-unit-testable) add it to vitest.config.ts coverage.exclude in the SAME PR. Paired update: shellNavHash.test.ts (+ optional vitest.config.ts exclude).
- coverage path reconciliation (Finding from anchor drift): the seam context files must live under a covered glob. Either place them under src/contexts/ (plural, already included) or add 'src/context/**' to vitest.config.ts coverage.include in-commit; otherwise the seam reducers silently escape the coverage gate. Paired update: vitest.config.ts and/or file relocation, recorded in the constraint doc.
- knip dead-code: the two new .mjs are referenced by package.json (via verify) so they are not dead; shellNavHash.ts must be imported by useAppNav.ts (used, not orphaned). No knip config change expected — verify with npm run check:dead-code.
- doc-drift (15-path FILE-PATH RUNTIME_COPY_RULES): Phase 7 edits NO guarded source file, so docs/doc-drift.rules.ts token pins are untouched. The NEW constraint doc must NOT hardcode the stale '13' — state 15 (or derive from RUNTIME_COPY_RULES.length); if it restates any machine-checked figure, pair it in docs/doc-drift.manifest.ts. Paired update: manifest.ts pairing only if the doc restates guarded truth.
- hostSizeBudget (5 hosts @400 LOC): unaffected — Phase 7 touches src/hooks/** and scripts/**, not the five named sheet hosts. Phase 7 formalizes this gate as of-record; re-run src/__tests__/hostSizeBudget.test.ts to confirm no regression.
- bundle-size: no new eager code of consequence (hash codec is tiny; gate scripts and e2e specs are build-excluded). Re-run npm run check:bundle-size to confirm no regression.
- chrome-dominance gate itself is a NET-NEW CI gate that MUST be anchored to post-Phase-6 state (Finding 20) — enabling it earlier would fail on the docked 20rem right rail or force the premature right-rail collapse Finding 20 forbids. Introduce it in this Phase-7 PR (which lands after Phase 6).

**Open questions:**
- Should transient modal overlays (e.g. NewCharacterDialog) be EXCLUDED from hash restore, or restored like persistent overlays (the dock)? The plan says restore `overlay`, but re-opening a creation modal on reload may be surprising UX. Recommend: restore only durable overlays (dock), drop transient dialogs on reload — needs a one-line decision in the seam doc.
- The chrome-dominance threshold (~10%) and the precise definition of 'non-content chrome' (52px header rail always; summoned tray only while open?) need a MEASURED baseline captured from the actual post-Phase-6 5b build before the number is hardcoded in check-chrome-dominance.mjs — otherwise the gate is either trivially green or flaky.
- Confirm the interaction-latency BUDGET value: Phase 1 was to record a baseline performance.measure number; Phase 7 promotes it to a gate. The exact ms/frame threshold must be pinned from that recorded baseline (with a tolerance) rather than invented, so the gate reflects the real measured floor.

---

## Amendment — 2026-07-14: Phase 1 as actually merged (PR #30)

**Status note.** Phase 1 did NOT land as the ONE atomic PR this spec mandates. PR #30 (merged 2026-07-09) landed a subset; everything below is verified against main `b0f0371` (2026-07-14). The Phase-1 sections above are preserved unedited as the plan of record for the remaining work — read them THROUGH this amendment. Task numbers below are the Phase-1 task numbers (1-14) above. Any gate elsewhere in this doc phrased as "confirm Phase 1 merged" (Phase 2's prerequisite-gap bullet, Phase 4 task 1, the per-phase prerequisite gates) must now be evaluated against the itemized list here, not against the merge of PR #30 — a bare "is P1 merged?" check would falsely pass.

### Landed in PR #30

- **Task 1 — landed in full, ahead of its consumers.** `src/hooks/useAppNav.ts` ships the total ShellNavState union (surface including `'scene'`, librarySegment, sheetDocId, sceneId, overlay), the local assertNever, openSheet/closeSheet/setSurface/setLibrarySegment, AND the `selectScene` action whose reducer arm sets sceneId and flips `surface:'scene'`. But the scene half is UNCONSUMED: grep finds no dispatcher of `selectScene`, no reader of `surface === 'scene'`, and no use of `nav.sceneId`/`onSelectScene` in App.tsx or AppHeader.tsx. The hook-side seam for task 3 exists; nothing feeds it.
- **Task 2 — landed.** The sheet-open writers route through `nav.openSheet(docId)` and the close sites through `nav.closeSheet()` (flushes preserved); `currentDoc` derives from `nav.sheetDocId`.
- **Task 4, first half — landed (mechanism deviates, see below).** Lazy-mount-on-first-visit: App.tsx tracks `hasVisitedScenes` (L101-107) and mounts the lazy SceneManager only after the first Scenes visit, keeping it mounted across switches. The 3-way performance.measure harness was NOT built.
- **Task 6 — landed.** `src/components/NewCharacterDialog.tsx`: creation-as-dialog.
- **Task 7, partially — header nav + ungated Import landed.** AppHeader carries the segment nav, renders Import in BOTH the sheet-open and no-sheet branches, and offers New Character with no sheet open. The Export/Delete re-home did NOT land (see Deferred).
- **Task 9, first half — landed.** Alt+N repointed to open NewCharacterDialog (App.tsx L395-397, guarded off the sheet surface). Alt+1/2/3 did NOT land.
- **Task 10 — landed in modified form.** The home cascade/hero/action bar are replaced by one Library segment view per `nav.librarySegment`, with SystemStatusDashboard re-hosted as the content segment and doc-drift tokens intact; BUT the scenes segment hosts the full SceneManager (App.tsx L637-658), not the spec'd LibraryScenesView.

### Deferred — verified absent on main b0f0371 (2026-07-14)

- **Task 3 (scene-selection lift): NOT landed.** All five scene-selection writers are still internal `setSelectedSceneId` calls in `src/components/SceneManager.tsx` — init L109, auto-reset L143, import-select L807, create-select L872, rail-click L926. No `selectedSceneId`/`onSelectScene` props exist. Since the hook side is done (task 1 above), the remaining lift is SceneManager/App/test work only.
- **Task 5 (LibraryScenesView): NOT landed.** No such file exists under `src/`; the scenes home is the full SceneManager, create/import UI included, rendered as a Library segment.
- **Task 5 residue — LEFT-rail relocation: NOT landed.** The 18rem left list rail is intact (`src/components/SceneManager.tsx` L885, `xl:grid-cols-[18rem_minmax(0,1fr)]`); the grid has not widened. The RIGHT 20rem operating rail is docked, as required.
- **Tasks 7-8 (Export/Delete re-home + per-card roster controls): NOT landed.** AppHeader still renders Export and Delete as inline icon buttons inside the `currentDoc`-gated block (L150), not a Sheet-surface overflow menu; CharacterListView has only the bulk Export All / Clear All controls (L89-101), and CharacterCard receives only onOpen/onClone (L113-119) — no per-card Export/Delete/Import overflow. (The old header location still works, so this is IA debt, not a capability regression.)
- **Task 9, second half (Alt+1/2/3): NOT landed.** The only alt-modified binding in App.tsx's shortcut array is Alt+N. It is also semantically blocked: the landed shell treats Scenes as a Library segment and `surface:'scene'` is unconsumed, so there is no third surface to switch to until task 3 lands.
- **Task 11 (instrumentation + sceneManagerChunk guard): NOT landed.** No performance.mark/measure exists in `src/` — the Phase-7 interaction-budget gate has no baseline number. There is no explicit assertion that the SceneManager chunk stays out of the eager index chunk; the only protection is indirect — the 80 KiB gzip `index-*` app-chunk budget in `scripts/check-bundle-size.mjs` (L21), whose comment notes it was re-tightened BECAUSE SceneManager is lazy. De-lazying would likely blow that budget, but nothing asserts the chunk boundary itself (Finding 17 is unguarded).
- **Task 12 (direct-SceneManager suite rewrites): outstanding by coupling.** The controlled-prop contract they rewrite against is task 3's, which never landed; SceneManager.test.tsx and capabilityScenarios.test.tsx still exercise internal selection ownership.
- **Task 14 (the eight phase1-*.spec Playwright acceptance gates): NOT landed.** `e2e/` contains no phase1 spec. Gates (c), (e), and (h) are unwritable until tasks 3+5 land; gate (a)'s frame-budget half also depends on the missing task-11 instrumentation.
- **Keepalive measurement harness (task 4's second half; blast-radius item sceneMountHide.perf.test.ts): never built.** The 3-way comparison (display:none vs visibility-hidden-only vs visibility+content-visibility:auto) that was to justify the hide mechanism does not exist.

### Deviation — keepalive mechanism violates the plan of record

The shipped keepalive hides the mounted SceneManager with `<div hidden={!isScenesSegment}>` (`src/App.tsx` L638). The HTML `hidden` attribute is UA-stylesheet `display:none` — precisely the mechanism task 4 above and the final plan's Findings 7+14 (`docs/design/ui-shell-redesign-final-plan.md`) forbid in favor of visibility:hidden + off-screen positioning. Because the measurement harness was also skipped, the deviation is UNMEASURED: the per-switch re-layout cost of the hidden grid is unknown. Treat this as a standing plan violation to be fixed, not a ratified change — and it must NOT be generalized as-is by Phase 2's SurfaceStage.

### Phase-2 prerequisite gate — corrected reading

Phase 2's PREREQUISITE GAP drift bullet was written when none of the Phase-1 artifacts existed; that is no longer the state, but "Phase 1 merged" still does not hold. Itemized:

- **MET:** `src/hooks/useAppNav.ts` total union (Phase 2 tasks 1-3 can lift its reducer verbatim); the compound openSheet/closeSheet writers; the header segment tabs (Phase 2 task 10's collapse target); a first-visit lazy-mount mechanism for SceneManager (non-compliant, below).
- **UNMET 1 — the compound five-writer scene-selection seam (task 3).** SceneManager still owns selection internally. Phase 2 task 8's SceneSurface prop-threading (`selectedSceneId` unchanged through the surface) and Phase 4 task 1's "lifted selectedSceneId" prerequisite are blocked on it.
- **UNMET 2 — LibraryScenesView (task 5) and the LEFT-rail relocation it triggers.** Phase 2's SceneSurface is spec'd to "carry the Phase-1 LEFT-rail relocation forward" — there is nothing to carry forward, and LibrarySurface's scenes segment would today host a full canvas, not a picker.
- **UNMET 3 — a plan-compliant keepalive.** Phase 2 task 8 generalizes "Phase 1's SceneManager first-visit mechanism" to all three surfaces via visibility:hidden; the mechanism that actually shipped uses the forbidden `display:none`, so generalizing it as-is would spread the Findings 7+14 violation to every surface. The swap must precede or accompany SurfaceStage.

Phase 2 has no explicit task-0 precondition gate (unlike Phase 5); its prerequisite lives in the drift bullet above, which must now be read per this list. The same three items gate Phase 4 task 1's "confirm Phases 1-3 merged" check on the Phase-1 side.

### Recommended next increment

The coupled **tasks 3+5+12**: lift the five-writer scene-selection seam to controlled props, create LibraryScenesView (relocating the L807/L872 writers into it per task 5), remove the LEFT 18rem rail so the grid widens, and rewrite SceneManager.test.tsx + capabilityScenarios.test.tsx in the same commit — the task-3/task-12 atomicity rule above still binds. Bundle the S-sized keepalive swap (App.tsx L638 `hidden` → visibility:hidden + off-screen positioning) into the same increment so Phase 2 has a compliant mechanism to generalize. The hook-side seam already existing shrinks the increment to SceneManager/App/test work; landing it unblocks tasks 7-8's re-home, task 14's gates (c)(e)(h), the missing third surface for Alt+1/2/3, and all three unmet Phase-2 prerequisites at once. The original atomic-landing rationale (every sub-task breaks the others' suites) applied to the full 14-task set; with the union and writers already merged, the remainder decomposes into the coupled increments above without half-wiring the nav union further.

